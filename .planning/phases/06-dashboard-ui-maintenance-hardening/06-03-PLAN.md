---
phase: 06-dashboard-ui-maintenance-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/scripts/pre-merge-check.sh
autonomous: true
requirements:
  - MAINT-04
  - MAINT-05
must_haves:
  truths:
    - "HIGH_RISK_FILES in pre-merge-check.sh contains both packages/database/src/migrations.ts and packages/http-api/src/handlers/accounts.ts"
    - "Every fork-specific v1.1 code block across all phases 3-6 carries a // FORK PATCH: comment"
    - "FORK PATCH annotation count is 27 or higher after Phase 6 additions"
  artifacts:
    - path: ".planning/scripts/pre-merge-check.sh"
      provides: "Extended HIGH_RISK_FILES array covering v1.1 fork-patched files"
      contains: "packages/database/src/migrations.ts"
  key_links:
    - from: ".planning/scripts/pre-merge-check.sh HIGH_RISK_FILES"
      to: "git diff upstream/main -- packages/database/src/migrations.ts"
      via: "shell array iteration"
      pattern: "HIGH_RISK_FILES"
---

<objective>
Update pre-merge-check.sh HIGH_RISK_FILES for MAINT-04, and perform the full v1.1 FORK PATCH annotation audit for MAINT-05.

Purpose: Closes the maintenance hardening requirements — ensures v1.1 fork-specific files are tracked and annotated before the milestone is marked done.
Output: Updated shell script + confirmed FORK PATCH coverage across all v1.1 modified files.
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dashboard-ui-maintenance-hardening/06-CONTEXT.md
@.planning/phases/06-dashboard-ui-maintenance-hardening/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend HIGH_RISK_FILES in pre-merge-check.sh (MAINT-04)</name>
  <files>.planning/scripts/pre-merge-check.sh</files>
  <read_first>
    - .planning/scripts/pre-merge-check.sh (read full file to locate the HIGH_RISK_FILES array and understand surrounding script logic)
  </read_first>
  <action>
    Locate the HIGH_RISK_FILES bash array in .planning/scripts/pre-merge-check.sh. Per CONTEXT.md D-04 (locked decision), it currently contains:
      "packages/providers/src/providers/openai/provider.ts"
      "packages/providers/src/providers/openrouter/provider.ts"
      "packages/types/src/account.ts"

    Add exactly two new entries (repo-root-relative paths, no leading slash, matching git diff --name-only output):
      "packages/database/src/migrations.ts"
      "packages/http-api/src/handlers/accounts.ts"

    The target state of the HIGH_RISK_FILES array:
      HIGH_RISK_FILES=(
        "packages/providers/src/providers/openai/provider.ts"
        "packages/providers/src/providers/openrouter/provider.ts"
        "packages/types/src/account.ts"
        "packages/database/src/migrations.ts"
        "packages/http-api/src/handlers/accounts.ts"
      )

    No other lines in the script change. Do not add config/src/index.ts — the locked decision (CONTEXT.md D-04) specifies only migrations.ts and http-api/src/handlers/accounts.ts.

    After editing, verify the change:
      grep "migrations.ts\|http-api/src/handlers/accounts.ts" .planning/scripts/pre-merge-check.sh

    Commit: improve(06-03): add v1.1 fork patch files to HIGH_RISK_FILES (MAINT-04)
  </action>
  <verify>
    <automated>grep "migrations.ts" /Users/thamw/development/local/better-ccflare/.planning/scripts/pre-merge-check.sh && grep "http-api/src/handlers/accounts.ts" /Users/thamw/development/local/better-ccflare/.planning/scripts/pre-merge-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - .planning/scripts/pre-merge-check.sh HIGH_RISK_FILES array contains "packages/database/src/migrations.ts"
    - .planning/scripts/pre-merge-check.sh HIGH_RISK_FILES array contains "packages/http-api/src/handlers/accounts.ts"
    - The array still contains all three original entries (openai/provider.ts, openrouter/provider.ts, account.ts)
    - No other lines in the script were modified
    - config/src/index.ts is NOT added (locked decision scope)
    - bash -n .planning/scripts/pre-merge-check.sh exits 0 (script syntax valid)
  </acceptance_criteria>
  <done>pre-merge-check.sh updated. HIGH_RISK_FILES contains 5 entries. Script syntax valid. MAINT-04 requirement met.</done>
</task>

<task type="auto">
  <name>Task 2: FORK PATCH annotation audit across all v1.1 files (MAINT-05)</name>
  <files></files>
  <read_first>
    - packages/database/src/migrations.ts (scan for openrouter_provider_preference block — must have // FORK PATCH:)
    - packages/database/src/migrations-pg.ts (scan for openrouter_provider_preference block — must have // FORK PATCH:)
    - packages/database/src/database-operations.ts (scan for setAccountOpenrouterProviderPreference — must have // FORK PATCH:)
    - packages/database/src/repositories/account.repository.ts (scan for openrouter_provider_preference UPDATE — must have // FORK PATCH:)
    - packages/http-api/src/handlers/accounts.ts (scan for createAccountOpenrouterProviderPreferenceHandler and DELETE handler — must have // FORK PATCH:)
    - packages/http-api/src/router.ts (scan for openrouter-provider-preference route — must have // FORK PATCH:)
    - packages/providers/src/providers/openrouter/provider.ts (scan for provider preference injection and cache injection blocks — must have // FORK PATCH:)
    - packages/types/src/account.ts (scan for openrouterProviderPreference type additions — must have // FORK PATCH:)
  </read_first>
  <action>
    This task performs a read-only audit. No code changes unless a missing // FORK PATCH: annotation is found.

    STEP 1 — Run the baseline annotation count:
      grep -rn "// FORK PATCH" /Users/thamw/development/local/better-ccflare/packages/ --include="*.ts" --include="*.tsx" | wc -l

    Expected baseline: 27 annotations across 12 files (verified during research phase). If Phase 6 Plans 01–02 have already run, the count will be higher.

    STEP 2 — Check each v1.1-modified file systematically. For each file below, run:
      grep -n "// FORK PATCH" <file>
    and confirm the fork-specific blocks in that file are annotated.

    Files to audit (phases 3–5 additions):
      packages/database/src/migrations.ts — openrouter_provider_preference column addition
      packages/database/src/migrations-pg.ts — openrouter_provider_preference PG column
      packages/database/src/database-operations.ts — setAccountOpenrouterProviderPreference facade method
      packages/database/src/repositories/account.repository.ts — preference UPDATE/SELECT
      packages/http-api/src/handlers/accounts.ts — GET field mapping + PUT handler + DELETE handler
      packages/http-api/src/router.ts — DELETE route registration
      packages/providers/src/providers/openrouter/provider.ts — cache injection + provider preference injection
      packages/providers/src/providers/openai/provider.ts — cache_write_tokens extraction
      packages/proxy/src/auto-refresh-scheduler.ts — openrouterProviderPreference references
      packages/types/src/account.ts — type additions and mapper

    Files to audit (phase 6 additions, after Plans 01–02):
      packages/dashboard-web/src/api.ts — putAccountOpenrouterProviderPreference and deleteAccountOpenrouterProviderPreference methods
      packages/dashboard-web/src/components/accounts/AccountOpenrouterProviderPreferenceDialog.tsx — file header + dialog logic
      packages/dashboard-web/src/components/accounts/AccountListItem.tsx — new dropdown item block
      packages/dashboard-web/src/components/AccountsTab.tsx — new dialog state, handlers, and JSX mount

    STEP 3 — For any fork-specific block found WITHOUT a // FORK PATCH: comment, add the annotation.
    The annotation format is: // FORK PATCH: <brief description> (<requirement ID>)
    Place it on the line immediately before the fork-specific code block (or as a file-header comment if the entire file is fork-specific).

    STEP 4 — Run the final count:
      grep -rn "// FORK PATCH" /Users/thamw/development/local/better-ccflare/packages/ --include="*.ts" --include="*.tsx" | wc -l

    The count must be ≥ 27 (baseline) + any new Phase 6 additions. Record the final count in the SUMMARY.

    STEP 5 — If any files were modified in STEP 3, run:
      bun run typecheck && bun run lint

    Commit (only if annotations were added): improve(06-03): complete FORK PATCH annotation audit (MAINT-05)
    If no annotations were missing, commit: improve(06-03): confirm FORK PATCH annotation audit complete (MAINT-05)
  </action>
  <verify>
    <automated>grep -rn "// FORK PATCH" /Users/thamw/development/local/better-ccflare/packages/ --include="*.ts" --include="*.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "// FORK PATCH" packages/ --include="*.ts" --include="*.tsx" returns ≥ 27 matches
    - packages/database/src/migrations.ts contains at least one // FORK PATCH: comment on or adjacent to the openrouter_provider_preference column addition
    - packages/http-api/src/handlers/accounts.ts contains // FORK PATCH: comment on or adjacent to the PUT handler (~line 3594)
    - packages/http-api/src/handlers/accounts.ts contains // FORK PATCH: comment on or adjacent to the DELETE handler (~line 3659)
    - packages/http-api/src/router.ts contains // FORK PATCH: comment on or adjacent to the DELETE route
    - packages/dashboard-web/src/api.ts contains // FORK PATCH: OpenRouter provider preference API methods (PROV-04) (if Plan 02 ran first)
    - packages/dashboard-web/src/components/accounts/AccountOpenrouterProviderPreferenceDialog.tsx contains // FORK PATCH: comment (if Plan 02 ran first)
    - bun run typecheck exits 0 if any files were modified
  </acceptance_criteria>
  <done>Annotation audit complete. All v1.1 fork-specific code blocks annotated. Final count ≥ 27. MAINT-05 requirement met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-merge-check.sh → git diff | Shell script passes file paths to git; paths must be exact repo-root-relative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | pre-merge-check.sh path accuracy | mitigate | Paths added match git diff --name-only output exactly (no leading slash, no trailing slash) |
| T-06-03-02 | Repudiation | FORK PATCH audit completeness | mitigate | grep count provides objective evidence of annotation coverage; audit recorded in SUMMARY |
</threat_model>

<verification>
grep "migrations.ts" .planning/scripts/pre-merge-check.sh → returns match
grep "http-api/src/handlers/accounts.ts" .planning/scripts/pre-merge-check.sh → returns match
bash -n .planning/scripts/pre-merge-check.sh → exits 0 (syntax valid)
grep -rn "// FORK PATCH" packages/ --include="*.ts" --include="*.tsx" | wc -l → ≥ 27
</verification>

<success_criteria>
pre-merge-check.sh updated with 5-entry HIGH_RISK_FILES. FORK PATCH annotation count ≥ 27. All v1.1 fork-specific code blocks confirmed annotated. MAINT-04 and MAINT-05 requirements met.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-ui-maintenance-hardening/06-03-SUMMARY.md`
</output>
