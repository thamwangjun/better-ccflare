---
phase: 10-type-wiring-cli-http-api-sse-sniffer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/provider-config.ts
  - packages/types/src/account.ts
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02

must_haves:
  truths:
    - "`bun run typecheck` passes with zero errors after the type union extensions"
    - "`PROVIDER_NAMES.OPENROUTER_ANTHROPIC` resolves to `\"openrouter-anthropic\"` at runtime"
    - "`AccountListItem.mode` and `AddAccountOptions.mode` both accept `\"openrouter-anthropic\"` without a TS error"
    - "`PROVIDER_CONFIG[PROVIDER_NAMES.OPENROUTER_ANTHROPIC]` satisfies `ProviderConfig` at compile time"
  artifacts:
    - path: "packages/types/src/provider-config.ts"
      provides: "PROVIDER_NAMES enum entry + PROVIDER_CONFIG record entry for openrouter-anthropic"
      contains: "OPENROUTER_ANTHROPIC"
    - path: "packages/types/src/account.ts"
      provides: "AccountListItem.mode and AddAccountOptions.mode union members"
      contains: '"openrouter-anthropic"'
  key_links:
    - from: "packages/types/src/provider-config.ts"
      to: "packages/types/src/account.ts"
      via: "ProviderName type derived from PROVIDER_NAMES values"
      pattern: 'PROVIDER_NAMES\[.*\]'
---

<objective>
Extend the two shared type files — `provider-config.ts` and `account.ts` — so that `"openrouter-anthropic"` is a valid `ProviderName`, has a matching `PROVIDER_CONFIG` entry, and appears in the two account-facing mode unions (`AccountListItem.mode` and `AddAccountOptions.mode`).

Purpose: This is the foundational wave. All downstream plans (CLI, HTTP API, sniffer, provider) depend on the string literal being part of the type system. Without it, every other file that uses these unions will emit TypeScript errors.

Output: Zero-error `bun run typecheck` after editing just these two files. The `PROVIDER_NAMES` and `PROVIDER_CONFIG` entries exist and satisfy the `as const satisfies Record<ProviderName, ProviderConfig>` constraint (per D-06).
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-CONTEXT.md
@.planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-RESEARCH.md

<interfaces>
<!-- Extracted from packages/types/src/provider-config.ts (verified 2026-06-04) -->

PROVIDER_NAMES is an `as const` object at line 15. Current members include
`OPENROUTER: "openrouter"`. Add `OPENROUTER_ANTHROPIC: "openrouter-anthropic"` after it.

PROVIDER_CONFIG is a record typed `as const satisfies Record<ProviderName, ProviderConfig>` at
line 147. Each entry uses `[PROVIDER_NAMES.X]: { requiresSessionTracking, supportsUsageTracking,
supportsOAuth, defaultEndpoint }`. The OPENROUTER entry ends around line 115.

ProviderConfig shape (inferred from existing entries):
```typescript
{
  requiresSessionTracking: boolean;
  supportsUsageTracking: boolean;
  supportsOAuth: boolean;
  defaultEndpoint: string;
}
```

<!-- Extracted from packages/types/src/account.ts (verified 2026-06-04) -->

AccountListItem.mode union at lines 270–286: includes `"openrouter"` at line 281.
AddAccountOptions.mode union at lines 296–305: includes `"openrouter"` at line 304.
Both use `|`-separated string literals.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend PROVIDER_NAMES, PROVIDER_CONFIG, and account mode unions (D-06)</name>
  <files>packages/types/src/provider-config.ts, packages/types/src/account.ts</files>
  <read_first>
    - packages/types/src/provider-config.ts — read the full file to locate PROVIDER_NAMES (line 15), the OPENROUTER entry in PROVIDER_CONFIG (ends ~line 115), and the satisfies constraint (line 147)
    - packages/types/src/account.ts — read lines 265–310 to locate AccountListItem.mode (line 281) and AddAccountOptions.mode (line 304)
    - .planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-CONTEXT.md — D-06 locked decision (provider config field values)
  </read_first>
  <action>
    CRITICAL: Both PROVIDER_NAMES and PROVIDER_CONFIG must be edited in the same atomic edit. Adding OPENROUTER_ANTHROPIC to PROVIDER_NAMES without adding the matching PROVIDER_CONFIG entry causes a TypeScript compile error at the `as const satisfies Record<ProviderName, ProviderConfig>` assertion on line 147.

    In packages/types/src/provider-config.ts:
    1. After the `OPENROUTER: "openrouter"` line in PROVIDER_NAMES, add:
       `// FORK PATCH: openrouter-anthropic provider name (D-06 / Phase 10)`
       `OPENROUTER_ANTHROPIC: "openrouter-anthropic",`
    2. After the OPENROUTER block in PROVIDER_CONFIG (the block keyed by `[PROVIDER_NAMES.OPENROUTER]`), add:
       `// FORK PATCH: openrouter-anthropic provider config (D-06 / Phase 10)`
       `[PROVIDER_NAMES.OPENROUTER_ANTHROPIC]: {`
       `  requiresSessionTracking: false,`
       `  supportsUsageTracking: false,`
       `  supportsOAuth: false,`
       `  defaultEndpoint: "https://openrouter.ai/api/v1",`
       `},`

    In packages/types/src/account.ts:
    3. In AccountListItem.mode union (line 281 area), after `| "openrouter"`, add:
       `| "openrouter-anthropic" // FORK PATCH: D-06 / Phase 10`
    4. In AddAccountOptions.mode union (line 304 area), after `| "openrouter"`, add:
       `| "openrouter-anthropic" // FORK PATCH: D-06 / Phase 10`

    Annotate all additions with `// FORK PATCH:` (CLAUDE.md requirement).
    Use named exports only; no `export default`.
  </action>
  <verify>
    <automated>cd /Users/thamw/development/local/better-ccflare && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/types/src/provider-config.ts PROVIDER_NAMES object contains `OPENROUTER_ANTHROPIC: "openrouter-anthropic"`
    - packages/types/src/provider-config.ts PROVIDER_CONFIG contains an entry keyed by `[PROVIDER_NAMES.OPENROUTER_ANTHROPIC]` with all four fields: requiresSessionTracking: false, supportsUsageTracking: false, supportsOAuth: false, defaultEndpoint: "https://openrouter.ai/api/v1"
    - packages/types/src/account.ts AccountListItem.mode union includes `"openrouter-anthropic"` as a member
    - packages/types/src/account.ts AddAccountOptions.mode union includes `"openrouter-anthropic"` as a member
    - `bun run typecheck` exits 0 with zero errors after this edit
  </acceptance_criteria>
  <done>Both files edited atomically; `bun run typecheck` passes with zero errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type definitions → compiled output | Type changes are compile-time only; no runtime attack surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-A | Tampering | PROVIDER_CONFIG entry | accept | Type-only change; no runtime data path affected. The `satisfies` constraint ensures shape correctness at compile time. |
| T-10-01-B | Information Disclosure | PROVIDER_CONFIG defaultEndpoint | accept | `"https://openrouter.ai/api/v1"` is a public, documented endpoint — not a secret. |
</threat_model>

<verification>
Run after plan completion:
- `bun run typecheck` — must exit 0
- `grep -n "openrouter-anthropic" packages/types/src/provider-config.ts packages/types/src/account.ts` — must appear in both files
- `grep -n "OPENROUTER_ANTHROPIC" packages/types/src/provider-config.ts` — must appear twice (PROVIDER_NAMES + PROVIDER_CONFIG key)
</verification>

<success_criteria>
`bun run typecheck` passes with zero errors after editing exactly two files. The `"openrouter-anthropic"` string literal is a valid `ProviderName` and member of both account mode unions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-01-SUMMARY.md`
</output>
