---
phase: 05-api-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts
autonomous: true
requirements:
  - PROV-03
must_haves:
  truths:
    - "Test file exists at packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts"
    - "All 11 test cases are present and importable"
    - "Tests for DELETE handler fail at import/compile time because createAccountOpenrouterProviderPreferenceDeleteHandler does not exist yet (D-07: RED gate — write failing tests first)"
    - "Tests for PUT handler fail because RED gate requires all tests to start failing (D-07)"
    - "bun test on the new file produces failures (not a passing run) (D-07)"
    - "All 5 required test case categories are present: PUT sets preference (returns 204), DELETE clears preference (returns 204, preference null after), PUT with invalid input (empty order / missing order / non-string items → 400), PUT on non-existent account (404), DELETE on non-existent account (404) (D-08)"
    - "Additional discretionary test cases present: allow_fallbacks defaulting to true (T-10) and explicit allow_fallbacks: false persisted (T-11) (D-09)"
  artifacts:
    - path: "packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts"
      provides: "Full TDD test suite for PUT + DELETE preference endpoints"
      min_lines: 150
  key_links:
    - from: "openrouter-provider-preference.test.ts"
      to: "packages/http-api/src/handlers/accounts.ts"
      via: "named imports createAccountOpenrouterProviderPreferenceHandler, createAccountOpenrouterProviderPreferenceDeleteHandler"
      pattern: "createAccountOpenrouterProviderPreferenceDeleteHandler"
---

<objective>
RED gate: Write the complete failing test suite for the PUT set-preference endpoint and the (not-yet-existing) DELETE clear-preference endpoint.

Purpose: Establish test contracts for PROV-03 before implementation. Tests import createAccountOpenrouterProviderPreferenceDeleteHandler from accounts.ts — which does not exist — so the test file will fail at import/compile time. The PUT tests also fail in this gate because no passing run is expected. Wave 2 (GREEN gate) makes all 11 tests pass.

Output: packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-layer/05-CONTEXT.md
@.planning/phases/05-api-layer/05-RESEARCH.md
@.planning/phases/05-api-layer/05-PATTERNS.md

<interfaces>
<!-- Canonical test pattern — mirror exactly from this file -->
From packages/http-api/src/handlers/__tests__/model-mappings-update.test.ts:
  - Uses bun:test (afterAll, beforeAll, beforeEach, describe, expect, it)
  - Uses node:fs (existsSync, unlinkSync)
  - Uses import type { DatabaseOperations } from "@better-ccflare/database"
  - Uses { DatabaseFactory } from "@better-ccflare/database"
  - TEST_DB_PATH = "/tmp/test-model-mappings-update.db"
  - insertAccount(dbOps, name) → inserts row into accounts table with raw SQL, returns UUID
  - beforeAll: unlinkSync if exists, DatabaseFactory.initialize(path), dbOps = DatabaseFactory.getInstance(), instantiate handlers
  - afterAll: DatabaseFactory.reset(), unlinkSync with try/catch
  - beforeEach: dbOps.getAdapter().run("DELETE FROM accounts", [])

From packages/http-api/src/handlers/accounts.ts (existing PUT handler shape, line ~3594):
  export function createAccountOpenrouterProviderPreferenceHandler(dbOps: DatabaseOperations)
    → returns async (req: Request, accountId: string): Promise<Response>
  Body: { order: string[], allow_fallbacks?: boolean }
  Validates: order is non-empty array of non-empty strings; allow_fallbacks defaults true
  404 guard: db.get("SELECT name FROM accounts WHERE id = ?", [accountId])
  Success: new Response(null, { status: 204 })

From packages/http-api/src/handlers/accounts.ts (DELETE handler — does NOT exist yet):
  export function createAccountOpenrouterProviderPreferenceDeleteHandler(dbOps: DatabaseOperations)
  → same factory shape, no body parsing
  → 404 guard same as PUT
  → calls dbOps.setAccountOpenrouterProviderPreference(accountId, null)
  → returns new Response(null, { status: 204 })

From packages/database/src/database-operations.ts (line ~750):
  async setAccountOpenrouterProviderPreference(accountId: string, preference: string | null): Promise<void>
  Accepts null — writes SQL NULL to openrouter_provider_preference column.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED gate test file</name>
  <files>packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts</files>

  <read_first>
    - packages/http-api/src/handlers/__tests__/model-mappings-update.test.ts (canonical pattern — mirror structure, imports, lifecycle exactly)
    - packages/http-api/src/handlers/accounts.ts lines 3594–3656 (existing PUT handler — understand validation rules, 404 guard, 204 response before writing tests)
    - packages/http-api/src/handlers/__tests__/ (directory listing — confirm no existing openrouter-provider-preference.test.ts)
  </read_first>

  <behavior>
    All 11 tests defined. 9 required (D-08) + 2 discretionary (T-10, T-11):

    T-01: PUT with { order: ["openai", "anthropic"] } → response.status === 204
    T-02: PUT with { order: ["openai", "anthropic"] } → readPreference(id).order deep equals ["openai", "anthropic"]
    T-03: DELETE on account with existing preference → response.status === 204
    T-04: DELETE on account with existing preference → readPreference(id) === null after call
    T-05: PUT with { order: [] } (empty array) → response.status === 400
    T-06: PUT with {} (missing order field) → response.status === 400
    T-07: PUT with { order: [123] } (non-string item) → response.status === 400
    T-08: PUT with non-existent accountId → response.status === 404
    T-09: DELETE with non-existent accountId → response.status === 404
    T-10: PUT with { order: ["openai"] } (no allow_fallbacks) → readPreference(id).allow_fallbacks === true
    T-11: PUT with { order: ["openai"], allow_fallbacks: false } → readPreference(id).allow_fallbacks === false
  </behavior>

  <action>
    Create packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts mirroring model-mappings-update.test.ts structure:

    1. Imports: bun:test lifecycle hooks + expect/it/describe; node:fs existsSync/unlinkSync; import type DatabaseOperations and DatabaseFactory from @better-ccflare/database; import BOTH createAccountOpenrouterProviderPreferenceHandler AND createAccountOpenrouterProviderPreferenceDeleteHandler from "../accounts" (the DELETE import is what causes compile failure in RED gate)

    2. Constant: TEST_DB_PATH = "/tmp/test-openrouter-provider-preference.db" (unique path — not the model-mappings path)

    3. Helper insertAccount(dbOps, name): raw SQL INSERT into accounts (id, name, provider, refresh_token, created_at, priority) with provider = "openrouter"; returns UUID

    4. Helper readPreference(dbOps, id): raw SQL SELECT openrouter_provider_preference FROM accounts WHERE id = ?; returns parsed JSON object { order, allow_fallbacks } or null if column is null

    5. Request factories: makePutRequest(body) creates a PUT Request to "http://localhost/api/accounts/x/openrouter-provider-preference" with Content-Type application/json and JSON.stringify(body) as body; makeDeleteRequest() creates a DELETE Request to same URL with no body

    6. beforeAll: if TEST_DB_PATH exists unlinkSync it; DatabaseFactory.initialize(TEST_DB_PATH); dbOps = DatabaseFactory.getInstance(); putHandler = createAccountOpenrouterProviderPreferenceHandler(dbOps); deleteHandler = createAccountOpenrouterProviderPreferenceDeleteHandler(dbOps)

    7. afterAll: DatabaseFactory.reset(); try { if exists unlinkSync } catch {}

    8. beforeEach: dbOps.getAdapter().run("DELETE FROM accounts", [])

    9. Write 11 it() blocks for T-01 through T-11. For each test that needs a pre-existing account: call insertAccount. For T-04 also set the preference first via a raw UPDATE or via putHandler call. For T-08 and T-09 pass "nonexistent-id" directly without calling insertAccount.

    Tabs for indentation, double quotes for strings (biome style).
  </action>

  <verify>
    <automated>bun test packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts 2>&1 | head -40</automated>
  </verify>

  <acceptance_criteria>
    - File exists at packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts
    - File imports createAccountOpenrouterProviderPreferenceDeleteHandler from "../accounts"
    - File imports createAccountOpenrouterProviderPreferenceHandler from "../accounts"
    - File contains exactly 11 it() blocks (T-01 through T-11)
    - TEST_DB_PATH is "/tmp/test-openrouter-provider-preference.db" (not the model-mappings path)
    - bun test on the file produces failures — not a clean pass. Expected failure: "createAccountOpenrouterProviderPreferenceDeleteHandler is not exported from ../accounts" or equivalent import error. Any passing run in this plan is a red flag — all tests should fail because the DELETE handler does not exist yet.
    - bun run typecheck exits non-zero (import of non-existent export causes type error) OR test run errors at import — either is correct RED gate behavior
  </acceptance_criteria>

  <done>
    Test file exists with 11 test cases. Running bun test on it produces failures due to missing createAccountOpenrouterProviderPreferenceDeleteHandler export. RED gate confirmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → handler | Tests call handler functions directly with Request objects — no network boundary |
| handler → DB | Handler calls dbOps.setAccountOpenrouterProviderPreference — SQL parameterized, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | PUT body deserialization | mitigate | order array validated: non-empty, all string items; allow_fallbacks coerced to boolean. Tests T-05/T-06/T-07 prove 400 for invalid input. |
| T-05-02 | Elevation of Privilege | PUT/DELETE on non-existent account | mitigate | 404 guard reads account from DB before any mutation. Tests T-08/T-09 prove 404 for missing account. |
| T-05-03 | Information Disclosure | Error messages in catch block | accept | Errors return generic message strings; no PII or internal DB details exposed in errorResponse output. |
| T-05-04 | Denial of Service | Large order array in PUT body | accept | No max-length enforcement on order array in this phase. Array is stored as JSON string; no compute-intensive processing. Low risk for operator-only endpoint. |
</threat_model>

<verification>
RED gate complete when:
- bun test packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts produces failures
- File has 11 test cases covering all D-08 required cases plus T-10/T-11
- Import of createAccountOpenrouterProviderPreferenceDeleteHandler is present (causes the failure)
</verification>

<success_criteria>
Test file written with 11 failing test cases. Running bun test on the file fails because createAccountOpenrouterProviderPreferenceDeleteHandler is not yet exported from accounts.ts. Wave 2 (GREEN gate) will implement the missing export and make all 11 tests pass.
</success_criteria>

<output>
After completion, create .planning/phases/05-api-layer/05-01-SUMMARY.md with:
- What was created (test file path, test count)
- Which tests exist by ID (T-01 through T-11)
- Confirmation that the run fails (RED gate confirmed)
- Any deviations from the plan
</output>
