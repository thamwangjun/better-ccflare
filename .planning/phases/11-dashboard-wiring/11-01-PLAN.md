---
phase: 11-dashboard-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard-web/src/components/accounts/AccountListItem.tsx
  - packages/dashboard-web/src/components/accounts/__tests__/AccountListItem.test.tsx
  - packages/dashboard-web/src/components/accounts/__tests__/AccountAddForm.test.tsx
autonomous: true
requirements: [MGMT-04]
must_haves:
  truths:
    - "An openrouter-anthropic account card renders the provider-preference settings button"
    - "An unrelated-provider account card does NOT render the provider-preference button"
    - "A failing SSR render test for the AccountAddForm new mode exists (RED) for Plan 02 to satisfy"
  artifacts:
    - path: "packages/dashboard-web/src/components/accounts/AccountListItem.tsx"
      provides: "Widened provider-preference gate including openrouter-anthropic"
      contains: 'account.provider === "openrouter-anthropic"'
    - path: "packages/dashboard-web/src/components/accounts/__tests__/AccountListItem.test.tsx"
      provides: "SSR test asserting the provider-preference button gate (MGMT-04)"
      contains: "renderToStaticMarkup"
    - path: "packages/dashboard-web/src/components/accounts/__tests__/AccountAddForm.test.tsx"
      provides: "SSR test asserting the new SelectItem label + form block render (MGMT-03 RED for Plan 02)"
      contains: "OpenRouter Anthropic Messages (API Key)"
  key_links:
    - from: "AccountListItem.tsx gate"
      to: "onProviderPreferenceChange button"
      via: "boolean OR condition"
      pattern: 'openrouter-anthropic.*onProviderPreferenceChange'
---

<objective>
Establish Wave 0 test coverage for both phase requirements and deliver MGMT-04 (the one-line provider-preference gate widen) in full.

Purpose: Tests must exist before implementation (Nyquist). MGMT-04 is a single boolean widen with no other surface, so it ships here alongside its own SSR test. The MGMT-03 form-render test is written here as a RED gate that Plan 02 turns GREEN.
Output: Widened gate in AccountListItem.tsx; two new SSR test files (AccountListItem.test.tsx — GREEN after this plan; AccountAddForm.test.tsx — RED until Plan 02).
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-wiring/11-CONTEXT.md
@.planning/phases/11-dashboard-wiring/11-RESEARCH.md
@.planning/phases/11-dashboard-wiring/11-PATTERNS.md
@.planning/phases/11-dashboard-wiring/11-VALIDATION.md

<interfaces>
SSR test harness — packages/dashboard-web/src/components/accounts/RateLimitProgress.test.tsx:7-36:
```tsx
import { describe, expect, it } from "bun:test";
import { renderToStaticMarkup } from "react-dom/server";
// const html = renderToStaticMarkup(<Component .../>); expect(html).toContain(...)
```

Pure-helper test header — packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts:1-8 (FORK PATCH header convention + named-export imports + bun:test).

Gate analog — AccountListItem.tsx:349-350:
```tsx
{/* FORK PATCH: Provider preferences button (PROV-04) */}
{account.provider === "openrouter" && onProviderPreferenceChange && ( ... )}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MGMT-04 SSR gate test (RED) for AccountListItem</name>
  <files>packages/dashboard-web/src/components/accounts/__tests__/AccountListItem.test.tsx</files>
  <read_first>
    - packages/dashboard-web/src/components/accounts/RateLimitProgress.test.tsx (SSR harness analog, lines 1-36)
    - packages/dashboard-web/src/components/accounts/AccountListItem.tsx (the component under test; read the props shape and the gate at lines 345-364, and the AccountListItem export signature for required props)
    - packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts (FORK PATCH test-header convention, lines 1-8)
  </read_first>
  <action>
    Create packages/dashboard-web/src/components/accounts/__tests__/AccountListItem.test.tsx. Start the file with a `// FORK PATCH: openrouter-anthropic provider-preference gate test (MGMT-04)` header (mirror the FORK PATCH header convention in AccountOpenrouterProviderPreferenceDialog.test.ts:1). Use the `renderToStaticMarkup` harness from RateLimitProgress.test.tsx (import from "react-dom/server"; import `describe, expect, it` from "bun:test"). Render AccountListItem three times against minimal valid props (construct the minimal AccountListItem prop object from the props read in the source; pass a non-null `onProviderPreferenceChange` callback). Assertions: (1) account.provider "openrouter-anthropic" → rendered HTML contains the provider-preference button marker (assert on a stable string from the existing button body at lines 351-364, e.g. its title/aria-label/visible text — pick the exact literal present in source); (2) account.provider "openrouter" → button present (regression guard); (3) account.provider "anthropic" (or another unrelated provider) → button absent. This test is expected to FAIL on assertion (1) until Task 3 widens the gate — that is the RED state. Use named imports only; no export default.
  </action>
  <verify>
    <automated>bun test packages/dashboard-web/src/components/accounts/__tests__/AccountListItem.test.tsx 2>&1 | grep -qE "(fail|FAIL|1 fail)" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - File __tests__/AccountListItem.test.tsx exists and begins with a `// FORK PATCH:` header
    - File imports `renderToStaticMarkup` from "react-dom/server" and `describe, expect, it` from "bun:test"
    - Contains three render assertions covering openrouter-anthropic (button present), openrouter (present), and one unrelated provider (absent)
    - Before Task 3, the openrouter-anthropic assertion FAILS (RED) — `bun test` for this file reports at least one failure
    - No `export default`
  </acceptance_criteria>
  <done>SSR test file exists, runs, and the openrouter-anthropic assertion is RED.</done>
</task>

<task type="auto">
  <name>Task 2: Write MGMT-03 SSR form-render test (RED) for AccountAddForm</name>
  <files>packages/dashboard-web/src/components/accounts/__tests__/AccountAddForm.test.tsx</files>
  <read_first>
    - packages/dashboard-web/src/components/accounts/RateLimitProgress.test.tsx (SSR harness analog)
    - packages/dashboard-web/src/components/accounts/AccountAddForm.tsx (props shape lines 17-134; SelectItem at line 1051; form block lines 1665-1748; required props for a minimal render)
  </read_first>
  <action>
    Create packages/dashboard-web/src/components/accounts/__tests__/AccountAddForm.test.tsx with a `// FORK PATCH: openrouter-anthropic add-account form test (MGMT-03)` header. Use the `renderToStaticMarkup` harness. Render AccountAddForm with minimal valid props (provide all required `onAddXxxAccount` callbacks AND a new `onAddOpenRouterAnthropicAccount` callback — this prop does not exist yet, which is part of the RED state; type it loosely or cast props so the test file itself compiles intent is to drive Plan 02). Assert the rendered HTML contains the verbatim SelectItem label string `OpenRouter Anthropic Messages (API Key)` (ROADMAP SC#1 / D-02 — exact casing, do not paraphrase). Because the dropdown renders options, the SelectItem text should appear in static markup; if Radix Select hides items in SSR, instead assert against the form block by also accepting the endpoint hint `https://openrouter.ai/api/v1 (native Anthropic Messages)` (D-03) — document in a comment which assertion is the GREEN target. This test is expected to FAIL until Plan 02 adds the SelectItem/form block. Named imports only.
  </action>
  <verify>
    <automated>bun test packages/dashboard-web/src/components/accounts/__tests__/AccountAddForm.test.tsx 2>&1 | grep -qE "(fail|FAIL)" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - File __tests__/AccountAddForm.test.tsx exists with a `// FORK PATCH:` header
    - Asserts the rendered HTML contains the exact string `OpenRouter Anthropic Messages (API Key)` (and/or the D-03 endpoint hint as documented fallback)
    - Test is RED before Plan 02 executes
    - No `export default`
  </acceptance_criteria>
  <done>Form-render SSR test exists and is RED, ready for Plan 02 to satisfy.</done>
</task>

<task type="auto">
  <name>Task 3: Widen the provider-preference gate (MGMT-04, D-05)</name>
  <files>packages/dashboard-web/src/components/accounts/AccountListItem.tsx</files>
  <read_first>
    - packages/dashboard-web/src/components/accounts/AccountListItem.tsx (lines 345-364 — the FORK PATCH comment at 349 and the gate at 350)
    - .planning/phases/11-dashboard-wiring/11-PATTERNS.md (Site 1 of AccountListItem section — the exact widen)
  </read_first>
  <action>
    In AccountListItem.tsx at the provider-preference gate (line 350, under the existing `// FORK PATCH: Provider preferences button (PROV-04)` comment at 349), change the condition from `account.provider === "openrouter" && onProviderPreferenceChange && (` to `(account.provider === "openrouter" || account.provider === "openrouter-anthropic") && onProviderPreferenceChange && (`. This is the entire MGMT-04 surface per D-05 — the button body (351-364), the onProviderPreferenceChange handler, the /openrouter-provider-preference endpoint, and the openrouterProviderPreference account field are all reused unchanged. Do NOT relabel the dialog (deferred). Update the existing FORK PATCH comment to note it now also covers openrouter-anthropic (MGMT-04), or add a sibling `// FORK PATCH: widened for openrouter-anthropic (MGMT-04)` line — keep the existing PROV-04 annotation.
  </action>
  <verify>
    <automated>bunx tsc --noEmit && bun test packages/dashboard-web/src/components/accounts/__tests__/AccountListItem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AccountListItem.tsx gate contains `account.provider === "openrouter-anthropic"` OR-ed with the existing openrouter check
    - A `// FORK PATCH:` annotation referencing MGMT-04 / openrouter-anthropic is present at the gate
    - `bunx tsc --noEmit` exits 0
    - The AccountListItem.test.tsx openrouter-anthropic assertion is now GREEN (all three assertions pass)
    - Button body and dialog component are unchanged (no relabel)
  </acceptance_criteria>
  <done>Gate widened, MGMT-04 SSR test GREEN, typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard UI → account-id-keyed REST endpoint | Provider-preference set/clear reuses the existing `/api/accounts/:id/openrouter-provider-preference` endpoint; no new boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | Widened gate exposing provider-preference button to openrouter-anthropic cards | accept | Endpoint is account-id-keyed and provider-agnostic (RESEARCH §Security, ARCHITECTURE §4); widening the client gate grants no capability the server does not already authorize per account. No new authorization path. |
| T-11-02 | Information Disclosure | Test fixtures containing fake API keys | accept | Tests construct dummy account props with no real secrets; SSR markup is not persisted or logged. |
</threat_model>

<verification>
- `bunx tsc --noEmit` exits 0
- `bun test packages/dashboard-web/src/components/accounts/` — AccountListItem.test.tsx all green; AccountAddForm.test.tsx RED (expected until Plan 02)
- Gate in AccountListItem.tsx includes openrouter-anthropic with FORK PATCH annotation
</verification>

<success_criteria>
- MGMT-04: an openrouter-anthropic account card renders the provider-preference button (SC#2 satisfied), proven by SSR test
- MGMT-03 form-render test exists as a RED gate for Plan 02
- No relabel of the provider-preference dialog (deferred idea respected)
- All additions carry `// FORK PATCH:` annotations
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-wiring/11-01-SUMMARY.md`
</output>
