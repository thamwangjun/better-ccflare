---
phase: 11-dashboard-wiring
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - packages/dashboard-web/src/api.ts
  - packages/dashboard-web/src/components/AccountsTab.tsx
  - packages/dashboard-web/src/components/accounts/AccountAddForm.tsx
autonomous: true
requirements: [MGMT-03]
must_haves:
  truths:
    - "The Add Account dropdown lists 'OpenRouter Anthropic Messages (API Key)' as a selectable mode"
    - "Selecting that mode reveals the API-key + model-mapping form block with the native-Anthropic endpoint hint"
    - "Submitting the form posts the account to POST /api/accounts/openrouter-anthropic and resets the form"
  artifacts:
    - path: "packages/dashboard-web/src/api.ts"
      provides: "addOpenRouterAnthropicAccount client method posting to /api/accounts/openrouter-anthropic"
      contains: "/api/accounts/openrouter-anthropic"
    - path: "packages/dashboard-web/src/components/AccountsTab.tsx"
      provides: "handleAddOpenRouterAnthropicAccount handler + form prop pass-through"
      contains: "handleAddOpenRouterAnthropicAccount"
    - path: "packages/dashboard-web/src/components/accounts/AccountAddForm.tsx"
      provides: "SelectItem, conditional form block, prop, submit-dispatch branch, four union extensions"
      contains: "OpenRouter Anthropic Messages (API Key)"
  key_links:
    - from: "AccountAddForm submit-dispatch (line ~710)"
      to: "onAddOpenRouterAnthropicAccount"
      via: 'if (newAccount.mode === "openrouter-anthropic")'
      pattern: 'mode === "openrouter-anthropic".*onAddOpenRouterAnthropicAccount'
    - from: "AccountsTab handler"
      to: "api.addOpenRouterAnthropicAccount"
      via: "await call"
      pattern: "api\\.addOpenRouterAnthropicAccount"
    - from: "api.addOpenRouterAnthropicAccount"
      to: "POST /api/accounts/openrouter-anthropic"
      via: "this.post"
      pattern: "/api/accounts/openrouter-anthropic"
---

<objective>
Deliver MGMT-03 in full: a user can add an openrouter-anthropic account via the dashboard Add Account form, end to end (dropdown → form block → submit → POST → form reset).

Purpose: Completes the dashboard-wiring success criterion SC#1. This is a verbatim mirror of the existing openrouter path across three files, plus the submit-dispatch branch that CONTEXT.md's canonical_refs omitted (the landmine).
Output: New SelectItem + form block + prop + submit branch + four union extensions in AccountAddForm.tsx; new addOpenRouterAnthropicAccount in api.ts; new handler + prop pass-through + union in AccountsTab.tsx. Turns Plan 01's AccountAddForm.test.tsx GREEN.
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-wiring/11-CONTEXT.md
@.planning/phases/11-dashboard-wiring/11-RESEARCH.md
@.planning/phases/11-dashboard-wiring/11-PATTERNS.md
@.planning/phases/11-dashboard-wiring/11-VALIDATION.md

<interfaces>
addOpenRouterAccount template — api.ts:481-505 (startTime, url, logger.debug(`→ POST ${url}`, { data }), this.post, duration log, catch+error log).
handleAddOpenRouterAccount template — AccountsTab.tsx:293-308 (try: await api call → loadAccounts → setAdding(false) → setActionError(null); catch: setActionError(formatError(err)); throw err).
Submit branch template — AccountAddForm.tsx:710-744 (api-key guard → modelMappings assembly → await onAddOpenRouterAccount({...}) → setNewAccount full reset 726-741 → onSuccess() → return).
Form block template — AccountAddForm.tsx:1665-1748 (password Input, endpoint hint <p> at 1682, Opus/Sonnet/Haiku model-mapping Inputs).
SelectItem template — AccountAddForm.tsx:1051 (`<SelectItem value="openrouter">OpenRouter (API Key)</SelectItem>`).
Four mode unions in AccountAddForm: props line 28, state line 157, accountParams cast line 431, onValueChange cast line 1020. Plus api.ts:259 and AccountsTab.tsx:120.
Submit target endpoint POST /api/accounts/openrouter-anthropic already exists (Phase 10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add addOpenRouterAnthropicAccount API client method + union (api.ts)</name>
  <files>packages/dashboard-web/src/api.ts</files>
  <read_first>
    - packages/dashboard-web/src/api.ts (lines 250-263 the initAddAccount mode union with "openrouter" at 259; lines 481-505 addOpenRouterAccount template; lines 1329-1361 provider-preference methods — confirm they are reused UNCHANGED)
    - .planning/phases/11-dashboard-wiring/11-PATTERNS.md (api.ts section, Sites 1-2)
  </read_first>
  <action>
    Two edits in api.ts. (1) Line 259: extend the initAddAccount mode param union to add `| "openrouter-anthropic"` with a `// FORK PATCH: openrouter-anthropic mode (MGMT-03)` annotation (type-only, mechanical per D-06/Open Question 1). (2) Directly below addOpenRouterAccount (after ~line 505): add a sibling `async addOpenRouterAnthropicAccount(data: { name: string; apiKey: string; priority: number; modelMappings?: { [key: string]: string } }): Promise<{ message: string; account: Account }>` whose body is identical to addOpenRouterAccount EXCEPT `const url = "/api/accounts/openrouter-anthropic"`. Mirror the startTime/logger.debug/this.post/duration-log/catch-with-error-log structure verbatim. Prefix the method with `// FORK PATCH: openrouter-anthropic account creation (MGMT-03)`. Do NOT touch the /openrouter-provider-preference methods (lines 1336/1361) — reused unchanged (D-05). Security: mirror the existing `logger.debug(`→ POST ${url}`, { data })` as-is; do NOT add new logging of the API key (T-11-03).
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - api.ts contains `addOpenRouterAnthropicAccount` posting to `/api/accounts/openrouter-anthropic`
    - The method signature matches addOpenRouterAccount (name, apiKey, priority, modelMappings?)
    - initAddAccount mode union at line ~259 includes `"openrouter-anthropic"`
    - Both additions carry `// FORK PATCH:` annotations
    - No new API-key logging beyond the mirrored `logger.debug(..., { data })`
    - `bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>API client method exists, posts to the correct endpoint, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add container handler + form prop pass-through + union (AccountsTab.tsx)</name>
  <files>packages/dashboard-web/src/components/AccountsTab.tsx</files>
  <read_first>
    - packages/dashboard-web/src/components/AccountsTab.tsx (lines 110-124 handleAddAccount union with "openrouter" at 120; lines 293-308 handleAddOpenRouterAccount template; line 644 form prop pass-through; lines 510-534 provider-preference handlers — confirm reused UNCHANGED; existing FORK PATCH at line 76)
    - .planning/phases/11-dashboard-wiring/11-PATTERNS.md (AccountsTab.tsx section, Sites 1-3)
    - packages/dashboard-web/src/api.ts (confirm addOpenRouterAnthropicAccount exists from Task 1)
  </read_first>
  <action>
    Three edits in AccountsTab.tsx. (1) Line 120: extend the handleAddAccount mode union with `| "openrouter-anthropic"` + FORK PATCH annotation. (2) Directly below handleAddOpenRouterAccount (~line 308): add a sibling `handleAddOpenRouterAnthropicAccount` with identical params (name, apiKey, priority, modelMappings?) and body, calling `api.addOpenRouterAnthropicAccount(params)` instead of `api.addOpenRouterAccount`. Mirror the try{ await api call; await loadAccounts(); setAdding(false); setActionError(null); } catch{ setActionError(formatError(err)); throw err; } structure verbatim. Prefix with `// FORK PATCH: openrouter-anthropic handler (MGMT-03)`. (3) Line 644: add a sibling form prop `onAddOpenRouterAnthropicAccount={handleAddOpenRouterAnthropicAccount}` directly next to `onAddOpenRouterAccount={handleAddOpenRouterAccount}`. Do NOT touch handleProviderPreferenceChange/handleSetProviderPreference/handleClearProviderPreference (510-534) — reused unchanged (D-05).
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AccountsTab.tsx contains `handleAddOpenRouterAnthropicAccount` calling `api.addOpenRouterAnthropicAccount`
    - The form receives `onAddOpenRouterAnthropicAccount={handleAddOpenRouterAnthropicAccount}` at line ~644
    - handleAddAccount mode union at line ~120 includes `"openrouter-anthropic"`
    - All additions carry `// FORK PATCH:` annotations
    - Provider-preference handlers unchanged
    - `bunx tsc --noEmit` exits 0 (NOTE: AccountAddForm prop type not yet added until Task 3 — typecheck may report the new prop until Task 3; run typecheck after Task 3 for the clean gate)
  </acceptance_criteria>
  <done>Container handler wired to the API method and passed to the form.</done>
</task>

<task type="auto">
  <name>Task 3: Add SelectItem, form block, prop, submit branch, and four unions (AccountAddForm.tsx)</name>
  <files>packages/dashboard-web/src/components/accounts/AccountAddForm.tsx</files>
  <read_first>
    - packages/dashboard-web/src/components/accounts/AccountAddForm.tsx (lines 17-32 props mode union @28; 99-134 onAddOpenRouterAccount prop + destructure; 146-162 state union @157; 422-432 accountParams cast @431; 710-744 submit branch — THE LANDMINE; 1011-1024 onValueChange cast @1020; 1045-1054 SelectItem @1051; 1665-1748 form block + endpoint hint @1682)
    - .planning/phases/11-dashboard-wiring/11-PATTERNS.md (AccountAddForm Sites 1-8, especially Site 5 the submit-dispatch landmine and Site 8 the D-03 hint edit)
    - .planning/phases/11-dashboard-wiring/11-RESEARCH.md (Pitfall 1, Pitfall 3, Pitfall 4; Assumption A1 — copy the setNewAccount reset tail verbatim)
    - packages/dashboard-web/src/components/accounts/__tests__/AccountAddForm.test.tsx (the RED test from Plan 01 — this task makes it GREEN)
  </read_first>
  <action>
    Eight edits in AccountAddForm.tsx, each with a `// FORK PATCH: ... (MGMT-03)` annotation. (Site 1) line 28: add `| "openrouter-anthropic"` to the props mode union. (Site 2) lines 99-104: add a sibling `onAddOpenRouterAnthropicAccount` prop with the same shape as onAddOpenRouterAccount ({name, apiKey, priority, modelMappings?} => Promise<void>); also add `onAddOpenRouterAnthropicAccount,` to the destructure at line 134. (Site 3) line 157: add `| "openrouter-anthropic"` to the newAccount state mode union. (Site 4) line 431: add `| "openrouter-anthropic"` to the accountParams cast union. (Site 5 — LANDMINE, Pitfall 1) at line 710 directly after the `if (newAccount.mode === "openrouter") {...}` branch: add a sibling `if (newAccount.mode === "openrouter-anthropic") {...}` branch copied verbatim from the openrouter branch (api-key guard, modelMappings assembly, await call, full setNewAccount reset lines 726-741 per Assumption A1, onSuccess(), return) — changing ONLY the error string to `"API key is required for OpenRouter Anthropic accounts"` and the call to `await onAddOpenRouterAnthropicAccount({...})`. (Site 6) line 1020: add `| "openrouter-anthropic"` to the onValueChange cast union. (Site 7) line 1051: add a sibling `<SelectItem value="openrouter-anthropic">OpenRouter Anthropic Messages (API Key)</SelectItem>` directly below the openrouter item — label VERBATIM per D-02/SC#1/Pitfall 4 (exact casing, keep `(API Key)`). (Site 8) lines 1665-1748: copy the entire `{newAccount.mode === "openrouter" && (...)}` form block verbatim as a `{newAccount.mode === "openrouter-anthropic" && (...)}` sibling, changing ONLY the endpoint hint at line 1682 from `Endpoint: https://openrouter.ai/api/v1` to `Endpoint: https://openrouter.ai/api/v1 (native Anthropic Messages)` per D-03/Pitfall 3 — leave all other strings/placeholders identical. After all edits run `bunx tsc --noEmit` to surface any missed union site (Pitfall 2 — five unions total counting api.ts/AccountsTab).
  </action>
  <verify>
    <automated>bunx tsc --noEmit && bun test packages/dashboard-web/src/components/accounts/</automated>
  </verify>
  <acceptance_criteria>
    - AccountAddForm.tsx contains `<SelectItem value="openrouter-anthropic">` with the exact text `OpenRouter Anthropic Messages (API Key)`
    - A sibling `if (newAccount.mode === "openrouter-anthropic")` submit branch exists at line ~710 that calls `onAddOpenRouterAnthropicAccount` AND includes the full setNewAccount reset + onSuccess() + return (submit-dispatch landmine closed)
    - The new form block's endpoint hint reads `Endpoint: https://openrouter.ai/api/v1 (native Anthropic Messages)`
    - All four AccountAddForm mode unions (lines ~28, ~157, ~431, ~1020) include `"openrouter-anthropic"`
    - New `onAddOpenRouterAnthropicAccount` prop declared and destructured
    - Every addition carries a `// FORK PATCH:` annotation
    - `bunx tsc --noEmit` exits 0
    - `bun test packages/dashboard-web/src/components/accounts/` exits 0 — AccountAddForm.test.tsx (from Plan 01) is now GREEN
  </acceptance_criteria>
  <done>Full MGMT-03 form path wired; submit branch present; form-render test GREEN; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → proxy REST endpoint | API key entered in the Add Account form is transmitted to the existing POST /api/accounts/openrouter-anthropic endpoint (same-origin via the dashboard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | API key echoed by `logger.debug(`→ POST ${url}`, { data })` in the mirrored api method | accept | Pre-existing behavior of addOpenRouterAccount (RESEARCH §Security). Mirror as-is; do NOT add new key logging. Key-redaction is a pre-existing concern out of this phase's scope — surfaced to user. |
| T-11-04 | Tampering | Submit branch dispatching to the wrong endpoint | mitigate | Dedicated one-handler-per-provider branch (D-04) posts only to /api/accounts/openrouter-anthropic; acceptance criterion asserts the exact endpoint string. No shared branch (anti-pattern). |
| T-11-05 | Information Disclosure | API-key Input field type | mitigate | Mirror the openrouter block's password-type Input verbatim (D-01) — the key is masked in the UI exactly as the existing provider. |
</threat_model>

<verification>
- `bunx tsc --noEmit` exits 0 (union completeness — Pitfall 2 gate)
- `bun test packages/dashboard-web/src/components/accounts/` exits 0 — both AccountListItem.test.tsx and AccountAddForm.test.tsx green
- `bun run lint && bun run typecheck && bun run format` clean (phase gate per CLAUDE.md)
- SelectItem label is verbatim `OpenRouter Anthropic Messages (API Key)`
- Submit-dispatch branch at AccountAddForm.tsx:710 present and calls onAddOpenRouterAnthropicAccount
</verification>

<success_criteria>
- MGMT-03 / SC#1: dropdown surfaces the exact label and submit posts to POST /api/accounts/openrouter-anthropic
- Submit-dispatch landmine closed (form does not silently no-op)
- D-03 endpoint hint applied; all other form strings identical to the openrouter block
- Every fork addition annotated `// FORK PATCH:`
- No deferred ideas implemented (no dialog relabel, no shared-component extraction)
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-wiring/11-02-SUMMARY.md`
</output>
