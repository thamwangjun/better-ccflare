---
quick_id: 260529-001
slug: merge-main-into-thamw-main
status: complete
commit: 1ef0bec5
date: 2026-05-29
---

# Quick Task 260529-001: Summary

## What was done

Merged `origin/main` into `thamw-main`. Upstream had 15 commits since the fork point (`a577de52`), including:
- `feat: adaptive rate-limit cooldown with exponential backoff` (PR #213)
- `fix: replace tee() with passthrough stream to prevent memory leak` (PR #238)
- `fix: stop auto-refresh probing manually-paused accounts` (PR #237)
- `fix: add periodic Bun.gc(true) in worker to reclaim structured-clone retention`

## Conflicts resolved (4 files, all keep-both)

Every conflict was the same pattern: fork's `openrouter_provider_preference` field vs upstream's new `consecutive_rate_limits` field. Both are additive — kept both.

| File | Sites |
|------|-------|
| `packages/database/src/migrations-pg.ts` | 1 (CREATE TABLE) |
| `packages/database/src/repositories/account.repository.ts` | 2 (SELECT lists) |
| `packages/proxy/src/auto-refresh-scheduler.ts` | 3 (stub Account objects) |
| `packages/types/src/account.ts` | 3 (AccountRow, Account, toAccount()) |

## Verification

- `git diff --check`: no conflict markers
- `bun run typecheck`: no new errors (pre-existing build-artifact errors only)
- `bun run lint`: 1 formatting fix applied to auto-refresh-scheduler.ts

## Commit

`1ef0bec5` — chore: merge origin/main — adaptive rate-limit cooldown, memory leak fix, manual-pause probe guard
