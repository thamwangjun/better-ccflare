---
phase: quick
plan: 260521-nd3
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/http-api/src/router.ts
  - packages/dashboard-web/src/components/accounts/AccountList.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CACHE-03, CACHE-04, CACHE-05, PROV-01, PROV-02, PROV-03, MAINT-04, MAINT-05]

must_haves:
  truths:
    - "router.ts PUT /openrouter-provider-preference block has a // FORK PATCH: annotation (matching adjacent DELETE block)"
    - "AccountList.tsx has a single // FORK PATCH: comment before the onProviderPreferenceChange prop"
    - "REQUIREMENTS.md traceability table has [x] for PROV-02, CACHE-03, CACHE-04, CACHE-05, PROV-01, PROV-03"
    - "REQUIREMENTS.md §CACHE-04 text clarifies the SYSTEM_PROMPT_CACHE_TTL_1H env-var gate"
    - "REQUIREMENTS.md §MAINT-04 text does not mention config/src/index.ts"
  artifacts:
    - path: "packages/http-api/src/router.ts"
      provides: "FORK PATCH annotation on PUT openrouter-provider-preference block"
    - path: "packages/dashboard-web/src/components/accounts/AccountList.tsx"
      provides: "FORK PATCH annotation before onProviderPreferenceChange prop"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Correct checkboxes, CACHE-04 clarification, MAINT-04 fix"
  key_links:
    - from: "router.ts line ~620"
      to: "// FORK PATCH: set OpenRouter provider preference"
      via: "comment line immediately before the PUT if-block"
    - from: "AccountList.tsx line ~18"
      to: "// FORK PATCH: thread provider preference change handler (PROV-04)"
      via: "comment line before onProviderPreferenceChange? prop declaration"
---

<objective>
Address all 5 tech debt items identified in the v1.1 milestone audit. All items are documentation-level: two missing FORK PATCH annotations in source files, and three REQUIREMENTS.md fixes (stale checkboxes, CACHE-04 env-var clarification, MAINT-04 stale description).

Purpose: Close the v1.1 milestone with zero outstanding tech debt before starting v1.2 planning.
Output: Three modified files. No logic changes — comments and docs only.
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FORK PATCH annotation to router.ts PUT block (TD-1)</name>
  <files>packages/http-api/src/router.ts</files>
  <action>
In `packages/http-api/src/router.ts`, find the comment on line 620:

```
// Account OpenRouter provider preference update
```

Replace it with two lines:

```
// FORK PATCH: set OpenRouter provider preference
// Account OpenRouter provider preference update
```

The result should be that the PUT if-block at ~line 620 now has a `// FORK PATCH:` annotation immediately before it, matching the pattern already present on the adjacent DELETE block at line 632.

Do NOT touch any other lines.
  </action>
  <verify>
    <automated>grep -n "FORK PATCH: set OpenRouter provider preference" /Users/thamw/development/local/better-ccflare/packages/http-api/src/router.ts</automated>
  </verify>
  <done>grep returns the line with `// FORK PATCH: set OpenRouter provider preference` within 2 lines of the PUT if-block for `/openrouter-provider-preference`</done>
</task>

<task type="auto">
  <name>Task 2: Add FORK PATCH annotation to AccountList.tsx (TD-2)</name>
  <files>packages/dashboard-web/src/components/accounts/AccountList.tsx</files>
  <action>
In `packages/dashboard-web/src/components/accounts/AccountList.tsx`, insert a single comment line immediately before line 19 (`onProviderPreferenceChange?: (account: Account) => void;`):

```
// FORK PATCH: thread provider preference change handler (PROV-04)
```

The interface block will then read:
```typescript
onModelMappingsChange?: (account: Account) => void;
// FORK PATCH: thread provider preference change handler (PROV-04)
onProviderPreferenceChange?: (account: Account) => void;
```

This one comment covers all three fork-modified lines (prop declaration in interface at line 19, destructure at line 40, and forwarding at line 86) with a single annotation at the declaration site. Do NOT add duplicate comments at lines 40 or 86.
  </action>
  <verify>
    <automated>grep -n "FORK PATCH: thread provider preference change handler" /Users/thamw/development/local/better-ccflare/packages/dashboard-web/src/components/accounts/AccountList.tsx</automated>
  </verify>
  <done>grep returns exactly one line with `// FORK PATCH: thread provider preference change handler (PROV-04)` immediately before the `onProviderPreferenceChange?` prop declaration</done>
</task>

<task type="auto">
  <name>Task 3: Fix REQUIREMENTS.md — checkboxes, CACHE-04 clarification, MAINT-04 description (TD-3, TD-4, TD-5)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make exactly three classes of changes to `.planning/REQUIREMENTS.md`:

**TD-4 — Mark 6 stale checkboxes as complete:**

Change these 6 lines from `[ ]` to `[x]`:
- `- [ ] **CACHE-03**:` → `- [x] **CACHE-03**:`
- `- [ ] **CACHE-04**:` → `- [x] **CACHE-04**:`
- `- [ ] **CACHE-05**:` → `- [x] **CACHE-05**:`
- `- [ ] **PROV-01**:` → `- [x] **PROV-01**:`
- `- [ ] **PROV-02**:` → `- [x] **PROV-02**:`
- `- [ ] **PROV-03**:` → `- [x] **PROV-03**:`

**TD-3 — Add env-var gate note to CACHE-04:**

Find the CACHE-04 line (now marked `[x]`):
```
- [x] **CACHE-04**: Proxy applies `ttl: "1h"` to tools and system `cache_control` blocks, and `{ type: "ephemeral" }` (5-min) to user message and last assistant turn blocks — coordinated with the existing `injectSystemCacheTtl` path in `proxy.ts`
```

Append a parenthetical to the end of the description, before the newline:
```
... in `proxy.ts` (note: the `ttl: "1h"` split on system/tools blocks is gated on `SYSTEM_PROMPT_CACHE_TTL_1H=true`; defaults to `false` — standard deploys only inject `{ type: "ephemeral" }`)
```

**TD-5 — Remove `config/src/index.ts` from MAINT-04 description:**

Find the MAINT-04 line:
```
- [x] **MAINT-04**: `pre-merge-check.sh` `HIGH_RISK_FILES` list updated to include all files receiving fork patches in v1.1 (`migrations.ts`, `config/src/index.ts`, `http-api/src/handlers/accounts.ts`)
```

Remove `, \`config/src/index.ts\`` from the parenthetical list, so it reads:
```
- [x] **MAINT-04**: `pre-merge-check.sh` `HIGH_RISK_FILES` list updated to include all files receiving fork patches in v1.1 (`migrations.ts`, `http-api/src/handlers/accounts.ts`)
```

**Traceability table:** Also update the 6 stale rows in the table from `Pending` to `Complete`:
- CACHE-03 | Phase 4 | Pending → Complete
- CACHE-04 | Phase 4 | Pending → Complete
- CACHE-05 | Phase 4 | Pending → Complete
- PROV-01 | Phase 4 | Pending → Complete
- PROV-02 | Phase 3 | Pending → Complete
- PROV-03 | Phase 5 | Pending → Complete

No other changes to the file.
  </action>
  <verify>
    <automated>grep -c "\[ \]" /Users/thamw/development/local/better-ccflare/.planning/REQUIREMENTS.md && grep -c "SYSTEM_PROMPT_CACHE_TTL_1H" /Users/thamw/development/local/better-ccflare/.planning/REQUIREMENTS.md && grep "MAINT-04" /Users/thamw/development/local/better-ccflare/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - `grep -c "[ ]"` returns 0 (no unchecked boxes remain)
    - `grep -c "SYSTEM_PROMPT_CACHE_TTL_1H"` returns 1 (env-var note added)
    - `grep "MAINT-04"` output does NOT contain `config/src/index.ts`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Comment-only and documentation changes; no data flows affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-nd3-01 | Tampering | router.ts | accept | Comment insertion only; no logic change; verified by grep |
</threat_model>

<verification>
After all 3 tasks:

1. `grep -n "FORK PATCH: set OpenRouter provider preference" packages/http-api/src/router.ts` — returns exactly 1 line within the PUT block
2. `grep -n "FORK PATCH: thread provider preference" packages/dashboard-web/src/components/accounts/AccountList.tsx` — returns exactly 1 line before `onProviderPreferenceChange?`
3. `grep -c "\[ \]" .planning/REQUIREMENTS.md` — returns 0
4. `grep "CACHE-04" .planning/REQUIREMENTS.md` — contains `SYSTEM_PROMPT_CACHE_TTL_1H`
5. `grep "MAINT-04" .planning/REQUIREMENTS.md` — does NOT contain `config/src/index.ts`
6. `bun run lint && bun run typecheck` — passes (comments only, no TS changes)
</verification>

<success_criteria>
- All 5 tech debt items from v1.1-MILESTONE-AUDIT.md resolved
- router.ts PUT block annotated (TD-1)
- AccountList.tsx prop declaration annotated (TD-2)
- REQUIREMENTS.md CACHE-04 clarified with env-var gate (TD-3)
- REQUIREMENTS.md 6 stale checkboxes marked [x] (TD-4)
- REQUIREMENTS.md MAINT-04 no longer mentions config/src/index.ts (TD-5)
- No logic changes — lint and typecheck pass unchanged
</success_criteria>

<output>
After completion, create `.planning/quick/260521-nd3-address-tech-debt-v11/260521-nd3-SUMMARY.md` following the summary template.
</output>
