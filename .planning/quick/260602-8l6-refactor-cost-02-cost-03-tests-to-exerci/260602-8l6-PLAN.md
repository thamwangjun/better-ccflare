---
phase: 260602-8l6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/proxy/src/usage-extraction.ts
  - packages/proxy/src/post-processor.worker.ts
  - packages/proxy/src/__tests__/sse-parsing.test.ts
  - packages/proxy/src/__tests__/extract-usage-from-json.test.ts
autonomous: true
requirements: [COST-02, COST-03]

must_haves:
  truths:
    - "Both test files import and exercise the real production functions, not inline copies"
    - "The SSE line parser used by tests is the same code path the worker runs"
    - "The cost-gating logic (provider-cost-vs-estimate, estimate-$0 -> undefined) tested by both files is the same code path handleEnd() runs"
    - "All previously-covered cases (present/absent/null/zero cost, gating, estimate-0, real-0) still pass"
    - "Generated inline-worker.ts is NOT edited"
    - "bun test on both files passes; lint, typecheck, format pass"
  artifacts:
    - path: "packages/proxy/src/usage-extraction.ts"
      provides: "Exported parseSSELine and resolveCostUsd alongside existing extract fns"
      contains: "export function resolveCostUsd"
    - path: "packages/proxy/src/__tests__/sse-parsing.test.ts"
      provides: "Tests calling real parseSSELine and resolveCostUsd"
    - path: "packages/proxy/src/__tests__/extract-usage-from-json.test.ts"
      provides: "Tests calling real resolveCostUsd"
  key_links:
    - from: "packages/proxy/src/post-processor.worker.ts"
      to: "packages/proxy/src/usage-extraction.ts"
      via: "import parseSSELine, resolveCostUsd"
      pattern: "import.*parseSSELine.*usage-extraction|resolveCostUsd"
---

<objective>
Eliminate the remaining inline-copy drift risk in the COST-02/COST-03 tests (tech-debt item #1, v1.2-MILESTONE-AUDIT). The `extractUsageFromData`/`extractUsageFromJson` copies were already removed — both test files import the real functions. What remains inline:
- `parseSSELine` (sse-parsing.test.ts lines 6-23) duplicates the worker's module-private parser.
- `resolveCostUsd` (sse-parsing.test.ts lines 151-163, extract-usage-from-json.test.ts lines 67-77) mirrors `handleEnd()` cost-gating; if `handleEnd` drifts, the tests stay green falsely.

Purpose: tests must exercise production code paths so future drift is caught.
Output: two new exported pure functions in usage-extraction.ts, the worker rewired to use them, both test files calling the real functions with identical case coverage.
</objective>

<context>
@packages/proxy/src/usage-extraction.ts
@packages/proxy/src/post-processor.worker.ts
@packages/proxy/src/__tests__/sse-parsing.test.ts
@packages/proxy/src/__tests__/extract-usage-from-json.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract parseSSELine and resolveCostUsd into usage-extraction.ts and rewire the worker</name>
  <files>packages/proxy/src/usage-extraction.ts, packages/proxy/src/post-processor.worker.ts</files>
  <read_first>packages/proxy/src/usage-extraction.ts, packages/proxy/src/post-processor.worker.ts</read_first>
  <action>
In usage-extraction.ts, add two exported functions:

1. `export function parseSSELine(line: string): { event?: string; data?: string }` — copy the body verbatim from post-processor.worker.ts (worker lines 234-251): event/data prefix handling with and without the space.

2. `export async function resolveCostUsd(state: UsageExtractionState, estimateCostUSD: () => Promise<number>): Promise<void>` — this is the pure cost-gating logic from handleEnd() (worker lines 539-553) WITHOUT the worker-specific estimate-args binding. Body (per D-04 — estimate-$0 must collapse to undefined so the writer's `?? null` yields null, while a real providerCostUsd === 0 survives):
   - if `state.usage.providerCostUsd !== undefined` → `state.usage.costUsd = state.usage.providerCostUsd`
   - else `const est = await estimateCostUSD(); state.usage.costUsd = est === 0 ? undefined : est;`
   The injected `estimateCostUSD` closure lets the worker pass its model/token args while tests pass a stub — keeping the gating logic shared.

In post-processor.worker.ts:
   - Replace the module-private `parseSSELine` definition (lines 234-251) by importing `parseSSELine` from "./usage-extraction" (extend the existing import on line 20). Delete the local definition.
   - In handleEnd() replace the inline gating block (lines 539-553) with a call: `await resolveCostUsd(state, () => estimateCostUSD(state.usage.model, { inputTokens: state.usage.inputTokens, outputTokens: finalOutputTokens, cacheReadInputTokens: state.usage.cacheReadInputTokens, cacheCreationInputTokens: state.usage.cacheCreationInputTokens }))`. Add `resolveCostUsd` to the import from "./usage-extraction". Keep the surrounding D-04 comment.
   - Do NOT touch inline-worker.ts (generated).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>usage-extraction.ts exports parseSSELine and resolveCostUsd; worker imports both and no longer defines its own parseSSELine or inline gating block; typecheck passes; inline-worker.ts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire both test files to call the real parseSSELine and resolveCostUsd</name>
  <files>packages/proxy/src/__tests__/sse-parsing.test.ts, packages/proxy/src/__tests__/extract-usage-from-json.test.ts</files>
  <read_first>packages/proxy/src/__tests__/sse-parsing.test.ts, packages/proxy/src/__tests__/extract-usage-from-json.test.ts</read_first>
  <action>
sse-parsing.test.ts:
   - Update the import (line 2) to also import `parseSSELine` and `resolveCostUsd` from "../usage-extraction".
   - Delete the inline `parseSSELine` function (lines 6-23). The existing `parseSSELine` describe block (lines 25-59) now exercises the real export unchanged — keep all 6 cases.
   - Delete the inline `resolveCostUsd` helper (lines 151-163). The four "handleEnd cost gating" cases (lines 165-193) now call the imported `resolveCostUsd`. Keep all four cases and their assertions (estimate-$0 -> undefined; real 0 survives; non-zero estimate preserved; estimate branch runs). Their state shapes already match UsageExtractionState structurally.

extract-usage-from-json.test.ts:
   - Update the import (line 2) to also import `resolveCostUsd` from "../usage-extraction".
   - Delete the inline `resolveCostUsd` helper (lines 67-77). The three "handleEnd estimate 0 -> undefined guard" cases (lines 79-100) now call the imported `resolveCostUsd`. Keep all three cases and assertions.
   - Leave the other extractUsageFromJson cases (present/absent/early-return/zero/feeds-costUsd) unchanged — they already call the real function.

Do not change any expected values or remove any case; only swap inline helpers for the real exports.
  </action>
  <verify>
    <automated>bun test sse-parsing.test.ts extract-usage-from-json.test.ts</automated>
  </verify>
  <done>Neither test file defines inline parseSSELine or resolveCostUsd; both import them from ../usage-extraction; all original test cases present and passing.</done>
</task>

<task type="auto">
  <name>Task 3: Full quality gate</name>
  <files>(no edits unless gate fails)</files>
  <action>Run the project quality gates. Fix any lint/format/type issues introduced by tasks 1-2. Confirm no edits leaked into generated files (inline-worker.ts, inline-vacuum-worker.ts, inline-integrity-check-worker.ts) — if any were touched, `git checkout -- <path>`.</action>
  <verify>
    <automated>bun run lint && bun run typecheck && bun run format</automated>
  </verify>
  <done>lint, typecheck, format all pass; bun test on both files green; generated worker files unchanged.</done>
</task>

</tasks>

<verification>
- `bun test sse-parsing.test.ts extract-usage-from-json.test.ts` — all cases pass
- `bun run lint && bun run typecheck && bun run format` — clean
- `grep -n "function parseSSELine" packages/proxy/src/post-processor.worker.ts` returns nothing (now imported)
- `git status` shows inline-worker.ts unmodified
</verification>

<success_criteria>
Both test files exercise the real production functions (extractUsageFromData, extractUsageFromJson, parseSSELine, resolveCostUsd) with no inline duplicates; the worker shares the same parseSSELine and cost-gating code as the tests; identical case coverage; all gates green; tech-debt item #1 resolved.
</success_criteria>

<output>
Create `.planning/quick/260602-8l6-refactor-cost-02-cost-03-tests-to-exerci/260602-8l6-SUMMARY.md` when done
</output>
