---
context: quick
quick_id: 260617-3fb
task: 1
total_tasks: 3
status: paused
last_updated: 2026-06-17T02:43:42.944Z
---

# Resume: Quick Task 260617-3fb — Restore async Worker-offloaded usage collector

Full `--full` pipeline (discuss → research → plan → plan-check) is **complete and committed**
at `76f02f97` on `thamw-main`. Only the 3 implementation tasks + post-exec gates remain.

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| One-shot mega-executor | First executor (worktree-isolated sonnet) exhausted its context just reading files; the change is ~2000–3000 LOC across 11 files | blocking | Dispatch a FRESH executor **per task** (1, then 2, then 3), not one for all three |
| Per-task worktree isolation | Task 2 depends on Task 1's test files; separate worktrees would split that state | blocking | Run executors **non-isolated** on `thamw-main` (sequential, shared on-disk branch) |
| Recovering the stale 1091-line worker verbatim | The old `post-processor.worker.ts` is large; cat-ing it burns context | advisory | RESEARCH.md: re-host today's hardened `usage-collector.ts` logic inside the worker; recover only the controller/protocol shape from `315440fa^` |

**Do not proceed until these are understood.**

<current_state>
On `thamw-main` @ `76f02f97`, clean tree. Planning artifacts (CONTEXT/RESEARCH/PLAN) committed.
Plan-checker returned PASSED (0 issues). The failed executor's orphan worktree was auto-removed.
Nothing implemented yet.
</current_state>

<remaining_work>
- Task 1 (not started): TDD red — write 6 failing test surfaces (worker message-contract via transferable + transfer-list; guard/regression no controller.error; aliasing/no-corruption; ordering off-pull; drain/shutdown; gated RSS soak). Deterministic via controller seam + pure usage-extraction; no real Worker except the gated soak.
- Task 2 (not started): implementation (green) — see HANDOFF.json remaining_tasks[1].
- Task 3 (not started): build step — mirror vacuum-worker pattern in apps/cli/package.json to regenerate inline-worker.ts, OMIT tiktoken; full lint+typecheck+format+bun test.
- Then: Step 6.25 code-review (gsd-code-reviewer), Step 6.5 verifier (gsd-verifier → 260617-3fb-VERIFICATION.md), Step 7 STATE.md "Quick Tasks Completed" row, Step 8 final docs commit `docs(quick-260617-3fb): ...`.
</remaining_work>

<decisions_made>
- Worker + transferable ArrayBuffers = HARD requirement (no async fallback).
- No @dqbd/tiktoken re-add (provider counts only).
- RSS soak test manual/opt-in (gated, skipped by default).
- Backpressure = buffer-until-ready, bounded (drop+log past cap, never block/tear down client).
</decisions_made>

## Required Reading (in order)
1. `260617-3fb-PLAN.md` — the 3 tasks with files/action/verify/done.
2. `260617-3fb-CONTEXT.md` — the 4 LOCKED decisions.
3. `260617-3fb-RESEARCH.md` — exact git refs (`315440fa^`) + transferable technique + build step + post-#245 hardening to preserve (with file:line).
4. `./CLAUDE.md` — never bump version, never curl Anthropic, git add specific files, inline-worker.ts is build-generated.

## Infrastructure State
- Branch `thamw-main` @ `76f02f97`, clean. No worktrees, no running servers.
- `workflow.use_worktrees=true` globally, but for THIS task use non-isolated execution (see anti-patterns).

<next_action>
Dispatch a fresh `gsd-executor` (model sonnet, NO worktree isolation) scoped to **Task 1 only**.
After it commits Task 1, repeat for Task 2, then Task 3. Then run the post-exec gates
(code-review → verify → STATE.md → docs commit). The full orchestration steps are in
`$HOME/.claude/gsd-core/workflows/quick.md` Steps 6.25 → 8.
</next_action>
