---
quick_id: 260531-l6i
title: Trim CLAUDE.md, keep important parts, remove redundant parts
status: planned
---

# Quick Task 260531-l6i: Trim CLAUDE.md

## Goal
Reduce CLAUDE.md size by removing redundant guidance while preserving every
load-bearing safety rule. Scope (per user): trim hand-written rules AND collapse
the verbose auto-generated GSD/GitNexus blocks. Marker comments
(`<!-- GSD:*-start/end -->`, `<!-- gitnexus:start/end -->`) MUST be preserved so
the generating tools can still locate and update their sections.

## Tasks

### Task 1 — Rewrite CLAUDE.md
- **files:** `CLAUDE.md`
- **action:**
  - Hand-written section (top ~160 lines):
    - Merge the triplicated "NEVER bump version" rule (Publishing to npm + Version Updates) into one spot.
    - Consolidate Git Refspecs + Branch Management refspec-collision guidance.
    - Remove the duplicated "Commit Message Categories" (kept once; also appears in conventions block).
    - Keep all critical safety rules verbatim in intent: never-curl-Anthropic, file exclusions, PG migration porting, DB paths, commands, Qwen.
  - Auto-generated blocks: collapse content to essentials, keeping marker comments exactly:
    - STACK: short bullet list of runtime/lang/key deps.
    - CONVENTIONS: key naming + style rules only.
    - ARCHITECTURE: layer summary + key entry points.
    - GitNexus: keep Always/Never Do rules + staleness note; drop large resource/CLI tables.
    - PROJECT / skills / workflow / profile: keep brief.
- **verify:** `inline-worker.ts` and other excluded files untouched; all marker comment pairs still present; file noticeably shorter.
- **done:** CLAUDE.md trimmed, markers intact, no critical rule lost.

## Notes
- No code changes — single doc file. No lint/typecheck needed (markdown only).
- Pre-existing uncommitted files (`.planning/STATE.md`, `.continue-here.md`) are NOT part of this task.
