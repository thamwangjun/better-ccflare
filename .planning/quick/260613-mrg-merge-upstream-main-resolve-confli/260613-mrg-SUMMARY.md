---
quick_id: 260613-mrg
status: complete
date: 2026-06-13
commit: be598d89
---

# Quick Task 260613-mrg: Resolve upstream/main merge — Summary

**Date:** 2026-06-13
**Commit:** be598d89 (merge commit, two parents)
**Status:** complete

## What was done

### Conflict 1: CLAUDE.md (both modified)
- Only the auto-managed GitNexus block (`<!-- gitnexus:start/end -->`) conflicted.
- **Resolution:** kept ours (HEAD) — the fork's condensed block. It is regenerated by
  `npx gitnexus analyze`, so the content choice is low-stakes.

### Conflict 2: post-processor.worker.ts (deleted by upstream)
- Upstream (`41752084`) deleted the post-processor Worker, replaced by
  `usage-collector.ts`. Our branch had added the `providerCostUsd` feature (real
  OpenRouter cost) to the deleted worker.
- **Resolution:** accepted the deletion (`git rm`) AND ported the feature forward:
  - `usage-collector.ts` now imports `extractUsageFromData`, `extractUsageFromJson`,
    `parseSSELine`, `resolveCostUsd` from `./usage-extraction` (removing its own inline
    copies that lacked the cost path).
  - Added `providerCostUsd?: number` to its `RequestState.usage`.
  - Replaced the direct `estimateCostUSD(...)` assignment with
    `resolveCostUsd(state, () => estimateCostUSD(...))` so a provider-returned cost wins
    over the estimate (and estimate-$0 collapses to null, not a persisted 0).

## Verification

- `bun run typecheck` — clean
- `npx biome lint usage-collector.ts usage-extraction.ts` — clean (no new warnings)
- `bun run format` — no fixes needed
- `bun test packages/proxy/src/__tests__/sse-parsing.test.ts extract-usage-from-json.test.ts` — 25/25 pass
- `bun test packages/proxy/` — 301 pass, 0 fail (2 pre-existing SQLITE teardown errors, unrelated)
- Merge finalized: `git log --merges` shows `be598d89`; no MERGE_HEAD remaining.

## Follow-ups

- Run `npx gitnexus analyze` to regenerate the CLAUDE.md GitNexus block / index.
- The `usage-extraction.ts` module is now the single source for cost/usage extraction;
  worker-era references in comments/tests are harmless.
