---
phase: quick-260619-l4e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: false
requirements: [MERGE-v3.5.27]
must_haves:
  truths:
    - "A merge commit from tag v3.5.27 exists on a fresh branch off thamw-main"
    - "CLAUDE.md retains all fork sections (safety rules, migration parity, Qwen/OpenRouter notes, git refspec rules)"
    - "Opus 4.8 and Fable 5 model registrations coexist with the openrouter-anthropic provider"
    - "SQLite and PG migrations are both present and consistent"
    - "Auto-generated inline worker files are unmodified vs thamw-main"
    - "lint, typecheck, format, and provider/database/proxy test suites are green"
  artifacts:
    - path: CLAUDE.md
      provides: "Fork project instructions with upstream additions folded in"
      contains: "NEVER curl the Anthropic endpoint"
  key_links:
    - from: "merge --no-ff v3.5.27"
      to: "new branch off thamw-main"
      via: "git merge"
      pattern: "Merge.*v3.5.27"
---

<objective>
Merge upstream tag v3.5.27 (18 commits) into a fresh branch off thamw-main, preserving the fork's OpenRouter-Anthropic provider work and cost-accounting rework.

Purpose: Stay current with upstream (Opus 4.8 + Fable 5 models, CLI wizard fallbacks, large-DB integrity fixes #259, Postgres idle-timeout/pool config #258, Greptile/GitNexus tooling) without losing fork-specific enhancements.
Output: A clean `--no-ff` merge commit on a new branch, with CLAUDE.md conflict resolved in favor of the fork, all overlap zones verified, validation green.
</objective>

<context>
@.planning/STATE.md
@./CLAUDE.md

Pre-flight (done): `git merge-tree` shows exactly ONE conflict — `CLAUDE.md`. All source files auto-merge.
Merge-base: commit 51094682. Current branch: thamw-main.

Critical CLAUDE.md rules governing this merge:
- NEVER bump version manually (release system handles root + apps/cli package.json + version.ts).
- NEVER edit auto-generated files: packages/proxy/src/inline-worker.ts, packages/database/src/inline-vacuum-worker.ts, packages/database/src/inline-integrity-check-worker.ts. If touched: `git checkout -- <path>`.
- Migration parity: every change in migrations.ts must have its counterpart in migrations-pg.ts.
- Git refspec: repo has both a `main` branch and `main` tag — irrelevant here since merging tag v3.5.27 onto a new local branch.
- Staging: `git add <specific-files>` only, never `git add .`.
- NEVER curl Anthropic during testing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fresh branch and run the merge</name>
  <files>(git branch + working tree)</files>
  <action>
From thamw-main (do NOT edit thamw-main directly), create a fresh branch:
`git checkout -b merge/upstream-v3.5.27`
Confirm the tag exists: `git tag -l v3.5.27` (and `git rev-list -n1 v3.5.27` to capture the tip).
Run the merge preserving history: `git merge --no-ff v3.5.27`
Expect exactly ONE conflict in CLAUDE.md (do not commit yet). Run `git status` to confirm CLAUDE.md is the only conflicted path; if any other path conflicts, STOP and surface it.
  </action>
  <verify>`git status` shows branch `merge/upstream-v3.5.27`, MERGE_HEAD present, CLAUDE.md the only unmerged path</verify>
  <done>On new branch, merge in progress, single conflict (CLAUDE.md) confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Resolve CLAUDE.md conflict (keep fork, fold in new upstream guidance)</name>
  <files>CLAUDE.md</files>
  <action>
Resolve the CLAUDE.md conflict by KEEPING the fork's content in full — all project instructions, the Critical Safety Rules (NEVER curl Anthropic, NEVER bump version, auto-generated file list), the Database Migration parity section, Git/refspec rules, Qwen/OpenRouter provider notes, GitNexus section, and Developer Profile placeholder.
Inspect the upstream side of each conflict hunk (`git show v3.5.27:CLAUDE.md` for reference). Fold in ONLY genuinely new, non-conflicting upstream guidance (e.g. new Greptile/GitNexus subagent tooling or post-commit auto-refresh notes) that does not contradict or weaken any fork rule. Drop nothing of the fork's.
Remove all conflict markers. Verify no `<<<<<<<`, `=======`, `>>>>>>>` remain: `grep -nE '^(<<<<<<<|=======|>>>>>>>)' CLAUDE.md` returns nothing.
Stage explicitly: `git add CLAUDE.md`.
  </action>
  <verify>`grep -cE '^(<<<<<<<|=======|>>>>>>>)' CLAUDE.md` returns 0; `grep -q "NEVER curl the Anthropic endpoint" CLAUDE.md` succeeds; `grep -q "MUST also be ported to" CLAUDE.md` succeeds</verify>
  <done>CLAUDE.md conflict resolved, fork sections intact, upstream additions folded, staged</done>
</task>

<task type="auto">
  <name>Task 3: Guard auto-generated files and verify overlap zones</name>
  <files>(verification only — restores auto-generated files if touched)</files>
  <action>
1. Guard auto-generated files. Check whether the merge touched them vs thamw-main:
   `git diff thamw-main -- packages/proxy/src/inline-worker.ts packages/database/src/inline-vacuum-worker.ts packages/database/src/inline-integrity-check-worker.ts`
   For ANY that differ, restore: `git checkout -- <path>` (then `git add <path>` if needed to keep them at thamw-main state). Per CLAUDE.md these are regenerated, never hand-edited.
2. Verify model registry overlap: grep the provider/model registration source (e.g. packages/providers/src/) for Opus 4.8 and Fable 5 registrations AND the openrouter-anthropic provider — confirm all coexist, nothing clobbered. `git diff thamw-main --stat -- packages/providers/` to scan.
3. Verify integrity + usage-accounting coexistence: inspect `git diff thamw-main` for integrity-scheduler.ts, usage-collector.ts, usage-extraction.ts — confirm upstream integrity changes and the fork usage-accounting refactor (providerCostUsd port) both present, no logic lost.
4. Verify migration parity: confirm both migrations.ts (SQLite) and migrations-pg.ts (PG) carry consistent changes — `git diff thamw-main -- packages/database/src/migrations.ts packages/database/src/migrations-pg.ts`. Any column/table added to one must exist in the other (ensureSchema/runMigrations ↔ ensureSchemaPg/runMigrationsPg).
5. Do NOT touch version files — confirm no manual version bump was introduced beyond what upstream merged (`git diff thamw-main -- package.json apps/cli/package.json packages/core/src/version.ts`; leave upstream's values as merged, do not hand-edit).
Report findings for each zone. If any silent clobbering or missing migration parity is found, STOP and surface before completing the merge.
  </action>
  <verify>Auto-generated files match thamw-main (`git diff thamw-main -- packages/proxy/src/inline-worker.ts packages/database/src/inline-vacuum-worker.ts packages/database/src/inline-integrity-check-worker.ts` empty); migration parity confirmed; Opus 4.8 / Fable 5 / openrouter-anthropic all present</verify>
  <done>Overlap zones verified clean, auto-generated files guarded, no silent clobbering</done>
</task>

<task type="auto">
  <name>Task 4: Validate (lint, typecheck, format, tests) then complete the merge commit</name>
  <files>(validation + merge commit)</files>
  <action>
Complete the merge commit first so validation runs against the resolved tree: `git commit --no-edit` (preserves the default merge message). Do NOT bump version.
Run validation in order:
- `bun run lint`
- `bun run typecheck`
- `bun run format`
- Provider/database/proxy test suites: `bun test packages/providers packages/database packages/proxy` (or the repo's standard test invocation for these packages). NEVER curl Anthropic; tests must use non-Anthropic paths.
If `bun run format` modifies files, stage them explicitly (`git add <files>`) and amend: `git commit --amend --no-edit`. If lint/typecheck/tests fail due to merge integration, fix minimally and amend.
Final check: `git log --oneline -1` shows the merge commit; `git status` clean.
  </action>
  <verify>`bun run lint && bun run typecheck` exit 0; provider/database/proxy tests pass; `git status` clean; `git log --oneline -1` shows merge of v3.5.27</verify>
  <done>Merge commit complete on merge/upstream-v3.5.27; lint+typecheck+format+tests green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Merge commit of upstream tag v3.5.27 onto fresh branch merge/upstream-v3.5.27, CLAUDE.md resolved in favor of the fork, overlap zones verified, validation green.</what-built>
  <how-to-verify>
1. `git log --oneline -5` — confirm the `--no-ff` merge commit referencing v3.5.27.
2. `git diff thamw-main --stat` — review the changed-file surface; confirm Opus 4.8 / Fable 5 / integrity / PG fixes present and no fork work dropped.
3. Open CLAUDE.md — confirm fork safety rules, migration parity section, Qwen/OpenRouter notes all intact.
4. Confirm `bun run lint && bun run typecheck` and the provider/database/proxy tests reported green.
Decide whether to merge this branch into thamw-main (manual, outside this plan).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Branch merge/upstream-v3.5.27 exists off thamw-main with a `--no-ff` merge commit from v3.5.27.
- CLAUDE.md retains every fork section; only conflict resolved.
- Auto-generated inline worker files identical to thamw-main.
- Migration parity (SQLite ↔ PG) intact; Opus 4.8 + Fable 5 + openrouter-anthropic provider coexist.
- lint, typecheck, format clean; provider/database/proxy tests green.
- No manual version bump.
</verification>

<success_criteria>
Clean merge commit on the new branch; CLAUDE.md retains fork sections; lint+typecheck+format+tests green; Opus 4.8 / Fable 5 / integrity / PG fixes all present; auto-generated files unmodified; no manual version bump.
</success_criteria>

<output>
After completion, create `.planning/quick/260619-l4e-merge-upstream-tag-v3-5-27-into-thamw-ma/260619-l4e-SUMMARY.md`
</output>
