---
phase: 10-type-wiring-cli-http-api-sse-sniffer
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - packages/cli-commands/src/commands/account.ts
  - packages/cli-commands/src/commands/help.ts
  - packages/http-api/src/handlers/accounts.ts
  - packages/http-api/src/router.ts
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02

must_haves:
  truths:
    - "CLI `--mode openrouter-anthropic` is accepted by `AddAccountOptionsWithAdapter.mode` union (typecheck gate)"
    - "CLI dispatch chain has an `else if (mode === 'openrouter-anthropic')` branch that creates a correctly typed account row"
    - "`--list` inference block returns `'openrouter-anthropic'` for accounts with that provider value"
    - "Help text lists `openrouter-anthropic` as a valid mode option with a description"
    - "`createOpenRouterAnthropicAccountAddHandler()` exists in accounts.ts and inserts `provider: 'openrouter-anthropic'`"
    - "`POST:/api/accounts/openrouter-anthropic` is registered in router.ts"
    - "`bun run typecheck` passes with zero errors after all four files are edited"
  artifacts:
    - path: "packages/cli-commands/src/commands/account.ts"
      provides: "CLI mode union extensions + dispatch branch + listAccounts inference"
      contains: '"openrouter-anthropic"'
    - path: "packages/cli-commands/src/commands/help.ts"
      provides: "Mode option documentation"
      contains: "openrouter-anthropic"
    - path: "packages/http-api/src/handlers/accounts.ts"
      provides: "createOpenRouterAnthropicAccountAddHandler factory"
      contains: "createOpenRouterAnthropicAccountAddHandler"
    - path: "packages/http-api/src/router.ts"
      provides: "Route registration for POST:/api/accounts/openrouter-anthropic"
      contains: "POST:/api/accounts/openrouter-anthropic"
  key_links:
    - from: "packages/http-api/src/router.ts"
      to: "packages/http-api/src/handlers/accounts.ts"
      via: "import createOpenRouterAnthropicAccountAddHandler and register at POST:/api/accounts/openrouter-anthropic"
      pattern: "openrouter-anthropic"
---

<objective>
Wire the CLI and HTTP API so users can create `openrouter-anthropic` accounts through both surfaces. This plan has two tasks aligned to the two wiring surfaces (CLI-side and HTTP-API-side) but they share no files and can be implemented sequentially in a single context window.

Purpose: MGMT-01 (CLI) and MGMT-02 (HTTP API) are the two user-facing account creation paths. Once these are wired, the `openrouter-anthropic` mode is fully registered in the system's operational layer.

Output: CLI dispatch branch + inference extension; help text updated; HTTP handler factory + route registration; `bun run typecheck` green.
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-CONTEXT.md
@.planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-RESEARCH.md
@.planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-01-SUMMARY.md

<interfaces>
<!-- From packages/cli-commands/src/commands/account.ts (verified 2026-06-04) -->

AddAccountOptionsWithAdapter.mode union: line 40–52, includes `| "openrouter"` at line 47.
AccountListItemWithMode.mode union: line 69–84, includes `| "openrouter"` at line 79.
CLI dispatch chain: `else if (mode === "openrouter")` branch at line 1334. The branch
  prompts for API key, priority, model mappings, then calls `createOpenRouterAccount(dbOps, ...)`.
listAccounts() inference: line 1659 contains `account.provider === "openrouter"` condition.

Template branch (lines 1334–1360): reads apiKey from prompt, reads priority from prompt,
  reads finalModelMappings, calls `createOpenRouterAccount(dbOps, name, apiKey, priority, finalModelMappings)`.
New branch needs: `createOpenRouterAnthropicAccount(dbOps, name, apiKey, priority, finalModelMappings)`
  — same shape, insert `provider: "openrouter-anthropic"`.

<!-- From packages/cli-commands/src/commands/help.ts (verified 2026-06-04) -->

Mode list string at line 9 — add `openrouter-anthropic` to the list.
Mode description table at line 20 — add a row for `openrouter-anthropic`.

<!-- From packages/http-api/src/handlers/accounts.ts (verified 2026-06-04) -->

createOpenRouterAccountAddHandler at line 3424: factory function accepting `dbOps`, reads
  `name`, `api_key`, `priority` from request JSON, inserts row with `provider: "openrouter"`,
  returns 200 + created account. Mirror this exactly, changing:
  - function name → `createOpenRouterAnthropicAccountAddHandler`
  - `provider: "openrouter"` → `provider: "openrouter-anthropic"`
  - success/log strings to reflect the new provider name

<!-- From packages/http-api/src/router.ts (verified 2026-06-04) -->

POST:/api/accounts/openrouter route at lines 248–249:
```typescript
this.handlers.set("POST:/api/accounts/openrouter", (req) =>
  openrouterAccountAddHandler(req),
);
```
Import of `createOpenRouterAccountAddHandler` is already present. Mirror: add import of
`createOpenRouterAnthropicAccountAddHandler` and register:
```typescript
// FORK PATCH: openrouter-anthropic account creation route (MGMT-02 / D-05)
this.handlers.set("POST:/api/accounts/openrouter-anthropic", (req) =>
  openrouterAnthropicAccountAddHandler(req),
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI mode union extensions + dispatch branch + help text (MGMT-01 / D-06)</name>
  <files>packages/cli-commands/src/commands/account.ts, packages/cli-commands/src/commands/help.ts</files>
  <read_first>
    - packages/cli-commands/src/commands/account.ts lines 40–85 — locate AddAccountOptionsWithAdapter.mode and AccountListItemWithMode.mode unions
    - packages/cli-commands/src/commands/account.ts lines 1330–1365 — locate the `else if (mode === "openrouter")` dispatch branch; read the full branch to understand what to mirror
    - packages/cli-commands/src/commands/account.ts lines 1650–1665 — locate the `listAccounts()` inference block and the `account.provider === "openrouter"` condition
    - packages/cli-commands/src/commands/help.ts lines 5–25 — locate the mode list string (line 9) and description table (line 20)
    - .planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-CONTEXT.md — D-06 for type union instructions
  </read_first>
  <action>
    In packages/cli-commands/src/commands/account.ts:

    1. AddAccountOptionsWithAdapter.mode union (~line 47): after `| "openrouter"` add:
       `| "openrouter-anthropic" // FORK PATCH: MGMT-01 / D-06`

    2. AccountListItemWithMode.mode union (~line 79): after `| "openrouter"` add:
       `| "openrouter-anthropic" // FORK PATCH: MGMT-01 / D-06`

    3. After the `else if (mode === "openrouter")` dispatch branch (~line 1334), add a sibling:
       ```
       // FORK PATCH: openrouter-anthropic CLI dispatch branch (MGMT-01 / D-06)
       } else if (mode === "openrouter-anthropic") {
       ```
       Mirror the `"openrouter"` branch exactly — prompt for apiKey, priority, finalModelMappings —
       then call `createOpenRouterAnthropicAccount(dbOps, name, apiKey, priority, finalModelMappings)`.
       Define `createOpenRouterAnthropicAccount()` as a private helper in the same file, mirroring
       `createOpenRouterAccount()` with `provider: "openrouter-anthropic"` in the DB insert.

    4. In the `listAccounts()` inference block (~line 1659): after `account.provider === "openrouter"`
       add `|| account.provider === "openrouter-anthropic"` so that `--list` returns the correct mode.
       Comment: `// FORK PATCH: MGMT-01 / D-06`

    In packages/cli-commands/src/commands/help.ts:

    5. Line 9 (mode option list string): add `openrouter-anthropic` to the list alongside other modes.
       Comment: `// FORK PATCH: MGMT-01 / D-06`

    6. Line 20 (description table): add a row:
       `openrouter-anthropic: OpenRouter native Anthropic Messages endpoint (API key)`
       Comment: `// FORK PATCH: MGMT-01 / D-06`

    All additions annotated with `// FORK PATCH:`. Use `Logger`, not `console.*`. Named exports only.
  </action>
  <verify>
    <automated>cd /Users/thamw/development/local/better-ccflare && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/cli-commands/src/commands/account.ts AddAccountOptionsWithAdapter.mode union contains `"openrouter-anthropic"` as a literal member
    - packages/cli-commands/src/commands/account.ts AccountListItemWithMode.mode union contains `"openrouter-anthropic"` as a literal member
    - packages/cli-commands/src/commands/account.ts contains an `else if (mode === "openrouter-anthropic")` dispatch branch after the `"openrouter"` branch
    - packages/cli-commands/src/commands/account.ts listAccounts() inference includes `account.provider === "openrouter-anthropic"` condition
    - packages/cli-commands/src/commands/help.ts contains "openrouter-anthropic" in both the mode list and description table
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>CLI accepts `--mode openrouter-anthropic`; dispatch branch exists; `--list` returns correct mode; help text updated; typecheck green</done>
</task>

<task type="auto">
  <name>Task 2: HTTP handler factory + route registration (MGMT-02 / D-05)</name>
  <files>packages/http-api/src/handlers/accounts.ts, packages/http-api/src/router.ts</files>
  <read_first>
    - packages/http-api/src/handlers/accounts.ts lines 3420–3470 — read the full `createOpenRouterAccountAddHandler` function to understand the exact template to mirror
    - packages/http-api/src/router.ts lines 1–20 — read imports to understand how handler factories are imported
    - packages/http-api/src/router.ts lines 245–255 — read the `POST:/api/accounts/openrouter` registration to mirror exactly
    - .planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-CONTEXT.md — D-05 locked decision
    - .planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-RESEARCH.md — Pitfall 3 (handler missing from router imports)
  </read_first>
  <action>
    In packages/http-api/src/handlers/accounts.ts:

    1. After the `createOpenRouterAccountAddHandler` function definition, add:
       ```
       // FORK PATCH: openrouter-anthropic account creation handler (MGMT-02 / D-05)
       export function createOpenRouterAnthropicAccountAddHandler(dbOps: DatabaseOperations) { ... }
       ```
       Mirror `createOpenRouterAccountAddHandler` exactly. Change only:
       - Function name → `createOpenRouterAnthropicAccountAddHandler`
       - `provider: "openrouter"` in the INSERT → `provider: "openrouter-anthropic"`
       - Log labels and success message strings to reference `openrouter-anthropic`
       Read the existing handler carefully — do not change the input shape (name, api_key, priority),
       validation logic, error handling, or response shape.

    In packages/http-api/src/router.ts:

    2. Import `createOpenRouterAnthropicAccountAddHandler` from the accounts handlers module
       (mirror the existing import of `createOpenRouterAccountAddHandler`).

    3. After the `POST:/api/accounts/openrouter` handler.set call (lines 248–249), add:
       ```
       // FORK PATCH: openrouter-anthropic account creation route (MGMT-02 / D-05)
       const openrouterAnthropicAccountAddHandler = createOpenRouterAnthropicAccountAddHandler(this.dbOps);
       this.handlers.set("POST:/api/accounts/openrouter-anthropic", (req) =>
         openrouterAnthropicAccountAddHandler(req),
       );
       ```

    All additions annotated with `// FORK PATCH:`. Named exports only (no `export default`).
    Do NOT change the existing `openrouter` handler or route.
  </action>
  <verify>
    <automated>cd /Users/thamw/development/local/better-ccflare && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/http-api/src/handlers/accounts.ts exports `createOpenRouterAnthropicAccountAddHandler` (named export)
    - The handler inserts `provider: "openrouter-anthropic"` in the DB row (visible in the function body)
    - packages/http-api/src/router.ts imports `createOpenRouterAnthropicAccountAddHandler`
    - packages/http-api/src/router.ts registers `"POST:/api/accounts/openrouter-anthropic"` in `this.handlers`
    - `bun run typecheck` exits 0
    - The existing `POST:/api/accounts/openrouter` route is unchanged (no regression)
  </acceptance_criteria>
  <done>HTTP handler factory exists and is exported; route registered in router; typecheck green; `openrouter` route unmodified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /api/accounts/openrouter-anthropic | Untrusted JSON body crosses into account creation handler |
| CLI user → account.ts dispatch | User-provided --mode string enters CLI dispatch chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-A | Tampering | createOpenRouterAnthropicAccountAddHandler input | mitigate | Mirror the existing `createOpenRouterAccountAddHandler` validation exactly — same name/api_key/priority validation. The existing handler validates and sanitizes all three fields; the new handler must replicate this, not skip it. |
| T-10-02-B | Elevation of Privilege | POST /api/accounts/openrouter-anthropic | mitigate | Mirror the existing route's auth posture exactly — the router enforces the same auth middleware for all `/api/accounts/*` routes. Verify the new route falls within the authenticated segment by checking router.ts auth middleware scope. |
| T-10-02-C | Spoofing | CLI mode string accepted without validation | accept | Mode union is enforced by TypeScript at compile time; runtime mode string comes from a trusted CLI flag (local user only, no network surface). |
| T-10-02-D | Information Disclosure | API key logged in handler | mitigate | Mirror existing handler — api_key is not logged. The new handler must follow the same pattern: log provider + name only, never the api_key value. |
</threat_model>

<verification>
Run after plan completion:
- `bun run typecheck` — must exit 0
- `grep -n "openrouter-anthropic" packages/http-api/src/router.ts` — must show the handler.set line
- `grep -n "createOpenRouterAnthropicAccountAddHandler" packages/http-api/src/handlers/accounts.ts packages/http-api/src/router.ts` — must appear in both files
- `grep -n "openrouter-anthropic" packages/cli-commands/src/commands/account.ts` — must appear in union, dispatch branch, and listAccounts inference
- `grep -n "openrouter-anthropic" packages/cli-commands/src/commands/help.ts` — must appear twice (list + table)
</verification>

<success_criteria>
`bun run typecheck` passes with zero errors. `createOpenRouterAnthropicAccountAddHandler` is exported and registered. CLI dispatch branch and `listAccounts()` inference cover the new mode. Help text documents it.
</success_criteria>

<output>
After completion, create `.planning/phases/10-type-wiring-cli-http-api-sse-sniffer/10-02-SUMMARY.md`
</output>
