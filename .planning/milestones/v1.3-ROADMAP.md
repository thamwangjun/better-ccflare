### Phase 9: Provider Class + Unit Tests

**Goal**: Deliver a new `OpenRouterAnthropicProvider` class with four method overrides (buildUrl, transformRequestBody, extractUsageInfo, extractStreamingUsage/parseUsage), proven correct via TDD (`bun:test`, mocked fetch) before any downstream wiring begins
**Depends on**: Phase 8 (cost persistence)
**Requirements**: PROV-01, PROV-02, PROV-03, CACHE-01, ROUTE-01, ROUTE-02, COST-01, COST-02
**Success Criteria** (what must be TRUE):

  1. `OpenRouterAnthropicProvider` extends `AnthropicCompatibleProvider` and routes to `https://openrouter.ai/api/v1/messages` with `Authorization: Bearer` auth
  2. Requests pass through with zero `cache_control` injection (verbatim passthrough)
  3. Provider-preference injection (`body.provider = { order, allow_fallbacks }`) mirrors v1.1 behavior exactly
  4. `session_id` is injected as a stable per-account hash when the client hasn't supplied one
  5. Real cost (`usage.cost`) is extracted on both streaming and non-streaming paths

**Plans**: 1 plan
Plans:

**Wave 1**

- [x] 09-01-PLAN.md — Wave 0 TDD: `OpenRouterAnthropicProvider` class, buildUrl override, transformRequestBody (3 FORK-PATCH injections), extractUsageInfo/extractStreamingUsage/parseUsage/readFinalSseCost, 34-test bun:test suite

---

### Phase 10: Type Wiring + CLI + HTTP API + SSE Sniffer

**Goal**: Propagate the `openrouter-anthropic` mode string across the full type chain, wire account creation via CLI and HTTP API, make mid-stream `overloaded_error` frames trigger account failover for the new provider, and add opt-in debug logging of `openrouter_metadata`
**Depends on**: Phase 9
**Requirements**: MGMT-01, MGMT-02, OBS-01, FAIL-01
**Success Criteria** (what must be TRUE):

  1. `openrouter-anthropic` is a valid `ProviderName` with a `PROVIDER_CONFIG` entry and propagates through all type-union sites
  2. User can add an `openrouter-anthropic` account via CLI (`--add-account --mode openrouter-anthropic`)
  3. User can add an `openrouter-anthropic` account via HTTP API (`POST /api/accounts/openrouter-anthropic`)
  4. The SSE sniffer includes `openrouter-anthropic` in its Anthropic-shape provider set, so mid-stream `overloaded_error` triggers failover
  5. `openrouter_metadata` (backend, latency) is logged at DEBUG level when `BETTER_CCFLARE_DEBUG` is set

**Plans**: 4 plans
Plans:

**Wave 1**

- [x] 10-01-PLAN.md — Type wiring: `PROVIDER_NAMES.OPENROUTER_ANTHROPIC`, `PROVIDER_CONFIG` entry, `AccountListItem.mode`/`AddAccountOptions.mode` unions

**Wave 2** *(depends on 10-01)*

- [x] 10-02-PLAN.md — CLI `--mode openrouter-anthropic` dispatch + `createOpenRouterAnthropicAccount` helper; HTTP `createOpenRouterAnthropicAccountAddHandler` + `POST /api/accounts/openrouter-anthropic` route
- [x] 10-03-PLAN.md — SSE sniffer: `openrouter-anthropic` added to `ANTHROPIC_SHAPE_PROVIDERS` for `overloaded_error` mid-stream failover
- [x] 10-04-PLAN.md — `X-OpenRouter-Experimental-Metadata: enabled` header injection + `openrouter_metadata` DEBUG logging tap on `message_stop`

---

### Phase 11: Dashboard Wiring

**Goal**: Users can create and manage `openrouter-anthropic` accounts entirely through the dashboard UI, with the provider-preference dialog available for the new account type
**Depends on**: Phase 10
**Requirements**: MGMT-03, MGMT-04
**Success Criteria** (what must be TRUE):

  1. The Add Account form surfaces `"OpenRouter Anthropic Messages (API Key)"` as a selectable mode option and successfully submits account creation to `POST /api/accounts/openrouter-anthropic`
  2. An `openrouter-anthropic` account card in the accounts list displays the provider-preference settings button (the dialog gate is widened from `openrouter`-only)

**Plans**: 2 plans
Plans:

**Wave 1**

- [x] 11-01-PLAN.md — Wave 0 SSR tests (MGMT-03 form-render RED, MGMT-04 gate) + the provider-preference gate widen (MGMT-04 complete)

**Wave 2** *(depends on 11-01: form-render test must exist first)*

- [x] 11-02-PLAN.md — MGMT-03 full add-account wiring: SelectItem + form block + submit branch (AccountAddForm), addOpenRouterAnthropicAccount (api.ts), handler + prop pass-through (AccountsTab)

**UI hint**: yes
