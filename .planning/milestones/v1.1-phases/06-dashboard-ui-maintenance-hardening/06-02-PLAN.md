---
phase: 06-dashboard-ui-maintenance-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - packages/dashboard-web/src/components/accounts/AccountOpenrouterProviderPreferenceDialog.tsx
  - packages/dashboard-web/src/api.ts
  - packages/dashboard-web/src/components/accounts/AccountListItem.tsx
  - packages/dashboard-web/src/components/accounts/AccountList.tsx
  - packages/dashboard-web/src/components/AccountsTab.tsx
  - packages/dashboard-web/src/components/accounts/index.ts
autonomous: true
requirements:
  - PROV-04
must_haves:
  truths:
    - "AccountOpenrouterProviderPreferenceDialog.tsx exists and exports the dialog plus three pure helper functions"
    - "The dialog's handleSave calls DELETE when input is empty, PUT when non-empty"
    - "The dropdown item 'Provider Preferences' renders only when account.provider === 'openrouter'"
    - "Allow fallbacks Switch defaults to ON (true)"
    - "AccountsTab mounts the dialog and wires both set and clear handlers"
    - "All Plan 01 TDD tests pass (GREEN gate)"
    - "bun run typecheck exits 0 after all changes"
  artifacts:
    - path: "packages/dashboard-web/src/components/accounts/AccountOpenrouterProviderPreferenceDialog.tsx"
      provides: "Provider preferences dialog component + exported pure helper functions"
      exports: ["AccountOpenrouterProviderPreferenceDialog", "parseProviderOrder", "resolveProviderPreferenceSaveAction", "syncProviderPreferenceState"]
    - path: "packages/dashboard-web/src/api.ts"
      provides: "putAccountOpenrouterProviderPreference + deleteAccountOpenrouterProviderPreference methods"
      contains: "FORK PATCH:"
    - path: "packages/dashboard-web/src/components/AccountsTab.tsx"
      provides: "Dialog state, handlers, dialog JSX mount"
      contains: "providerPreferenceDialog"
  key_links:
    - from: "AccountsTab.tsx handleSetProviderPreference"
      to: "api.putAccountOpenrouterProviderPreference"
      via: "direct async call"
      pattern: "putAccountOpenrouterProviderPreference"
    - from: "AccountsTab.tsx handleClearProviderPreference"
      to: "api.deleteAccountOpenrouterProviderPreference"
      via: "direct async call"
      pattern: "deleteAccountOpenrouterProviderPreference"
    - from: "AccountListItem.tsx dropdown item"
      to: "account.provider === 'openrouter' gate"
      via: "conditional render"
      pattern: "account\\.provider.*openrouter"
---

<objective>
Implement the Provider Preferences dialog and wire it throughout the dashboard, turning Plan 01's RED tests GREEN.

Purpose: Delivers PROV-04 — operators can configure and clear per-account OpenRouter provider preferences through the dashboard UI.
Output: Dialog component + API client methods + full prop threading from AccountsTab through AccountList to AccountListItem.
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dashboard-ui-maintenance-hardening/06-CONTEXT.md
@.planning/phases/06-dashboard-ui-maintenance-hardening/06-PATTERNS.md
@.planning/phases/06-dashboard-ui-maintenance-hardening/06-UI-SPEC.md

Prior plan output (test file for GREEN gate):
@.planning/phases/06-dashboard-ui-maintenance-hardening/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts extracted from codebase. Executor uses these directly. -->

From packages/types/src/account.ts:
```typescript
// AccountResponse.openrouterProviderPreference shape (verified Phase 3/4)
openrouterProviderPreference: {
  order: string[];
  allowFallbacks: boolean;
} | null;
```

From packages/dashboard-web/src/components/accounts/AccountModelMappingsDialog.tsx (dialog template):
```typescript
// Props interface pattern to mirror — split into two callbacks:
interface AccountModelMappingsDialogProps {
  isOpen: boolean;
  account: Account | null;
  onOpenChange: (open: boolean) => void;
  onUpdateModelMappings: (accountId: string, modelMappings: {...}) => Promise<void>;
}
// useEffect dependency: [account]
// handleSave: setIsLoading(true) → try/catch → onUpdate → onOpenChange(false) → finally setIsLoading(false)
// Early return: if (!account) return null;
// Cancel button: variant="outline", label "Discard Changes" (per UI-SPEC)
```

From packages/dashboard-web/src/components/accounts/AccountListItem.tsx (prop patterns):
```typescript
// Existing optional props to mirror (lines 55-56):
onCustomEndpointChange?: (account: Account) => void;
onModelMappingsChange?: (account: Account) => void;
// Separator gate (line 204):
{(onCustomEndpointChange || onModelMappingsChange) && <DropdownMenuSeparator />}
// Model mappings dropdown item (lines 227-246): template to copy for new item
```

From packages/dashboard-web/src/components/accounts/AccountList.tsx (prop threading):
```typescript
// Props (lines 17-18) + destructure (37-38) + forward (82-83): add onProviderPreferenceChange in all three
```

From packages/dashboard-web/src/components/AccountsTab.tsx (wiring):
```typescript
// Dialog state (lines 68-74): useState<{ isOpen: boolean; account: Account | null }>
// Open handler (lines 496-498): setDialog({ isOpen: true, account })
// Async handler (lines 537-548): try { await api.method(); await loadAccounts(); } catch { setActionError; throw; }
// AccountList prop (line 634): onModelMappingsChange={handleModelMappingsChange}
// Dialog mount (lines 701-712): conditional render pattern
```

From packages/dashboard-web/src/api.ts (API method patterns):
```typescript
// PUT method base: this.put(url, body) — uses updateAccountModelMappings pattern (lines 1301-1327)
// DELETE method base: this.delete(url) — uses deleteCombo pattern (line 1882-1883, no body)
// Method logging template: startTime, logger.debug arrow/back, logger.error on catch
// Error re-throw: if (error instanceof HttpError) throw new Error(error.message); throw error;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AccountOpenrouterProviderPreferenceDialog component and turn tests GREEN</name>
  <files>
    packages/dashboard-web/src/components/accounts/AccountOpenrouterProviderPreferenceDialog.tsx
    packages/dashboard-web/src/components/accounts/index.ts
  </files>
  <read_first>
    - packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts (the failing tests — implement against these exactly)
    - packages/dashboard-web/src/components/accounts/AccountModelMappingsDialog.tsx (canonical template to clone — full file)
    - packages/dashboard-web/src/components/ui/switch.tsx (Switch component API — confirm props: checked, onCheckedChange, id)
    - packages/dashboard-web/src/components/accounts/index.ts (current exports to extend)
    - .planning/phases/06-dashboard-ui-maintenance-hardening/06-UI-SPEC.md (copywriting, layout, spacing contracts)
  </read_first>
  <behavior>
    - parseProviderOrder("a, b") → ["a", "b"]
    - parseProviderOrder("") → []
    - parseProviderOrder("  ") → []
    - resolveProviderPreferenceSaveAction([]) → "clear"
    - resolveProviderPreferenceSaveAction(["p"]) → "set"
    - syncProviderPreferenceState(null) → { providerOrder: "", allowFallbacks: true }
    - syncProviderPreferenceState({ openrouterProviderPreference: null }) → { providerOrder: "", allowFallbacks: true }
    - syncProviderPreferenceState({ openrouterProviderPreference: { order: ["p1","p2"], allowFallbacks: false } }) → { providerOrder: "p1, p2", allowFallbacks: false }
  </behavior>
  <action>
    Create packages/dashboard-web/src/components/accounts/AccountOpenrouterProviderPreferenceDialog.tsx.

    File header (first line after imports): // FORK PATCH: Provider preferences dialog — PROV-04

    Export three pure helper functions (these are what the tests import):
      export function parseProviderOrder(input: string): string[] — splits on comma, trims, filters empty
      export function resolveProviderPreferenceSaveAction(parsed: string[]): "set" | "clear" — returns "clear" if length 0, "set" otherwise
      export function syncProviderPreferenceState(account: Account | null): { providerOrder: string; allowFallbacks: boolean } — reads openrouterProviderPreference, returns "" and true if null/absent

    Props interface: AccountOpenrouterProviderPreferenceDialogProps with:
      isOpen: boolean
      account: Account | null
      onOpenChange: (open: boolean) => void
      onSetProviderPreference: (accountId: string, order: string[], allowFallbacks: boolean) => Promise<void>
      onClearProviderPreference: (accountId: string) => Promise<void>

    Component state: providerOrder (string, ""), allowFallbacks (boolean, true), isLoading (boolean, false)

    useEffect with [account] dependency: call syncProviderPreferenceState(account), set both state values from result

    handleSave: guard on !account, setIsLoading(true), parseProviderOrder(providerOrder), branch on resolveProviderPreferenceSaveAction result:
      "clear" → await onClearProviderPreference(account.id)
      "set" → await onSetProviderPreference(account.id, parsed, allowFallbacks)
    After success: onOpenChange(false). In catch: console.error. In finally: setIsLoading(false).

    Early return: if (!account) return null

    JSX layout per UI-SPEC Layout Contract:
      DialogContent className="sm:max-w-[500px]"
      DialogHeader with DialogTitle "Provider Preferences" and DialogDescription "Configure OpenRouter provider routing for {account.name}."
      Content div className="space-y-4 py-2" (NO overflow-y-auto — dialog is small enough)
        Provider order field group: div className="space-y-1"
          Label htmlFor="provider-order" text "Provider Order"
          Input id="provider-order" value={providerOrder} onChange placeholder "e.g., anthropic/claude-3-5-sonnet, openai/gpt-4o"
          p className="text-xs text-muted-foreground" text "Comma-separated list. Leave empty to clear the preference."
        Allow fallbacks row: div className="flex items-center gap-2"
          Label htmlFor="allow-fallbacks" text "Allow fallbacks"
          Switch id="allow-fallbacks" checked={allowFallbacks} onCheckedChange={setAllowFallbacks}
      DialogFooter (no mt-2 shrink-0 unless the template uses it — check AccountModelMappingsDialog)
        Button variant="outline" onClick={() => onOpenChange(false)} disabled={isLoading} text "Discard Changes"
        Button onClick={handleSave} disabled={isLoading} text isLoading ? "Saving..." : "Save Changes"

    Imports: React and useState from "react", import type { Account } from "../../api", Button from "../ui/button", Dialog/DialogContent/DialogDescription/DialogFooter/DialogHeader/DialogTitle from "../ui/dialog", Input from "../ui/input", Label from "../ui/label", Switch from "../ui/switch"

    Export: named export only (no default). export function AccountOpenrouterProviderPreferenceDialog(...)

    After creating the dialog, extend packages/dashboard-web/src/components/accounts/index.ts with:
      export { AccountOpenrouterProviderPreferenceDialog } from "./AccountOpenrouterProviderPreferenceDialog";

    Run tests to confirm GREEN:
      bun test packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts

    Run typecheck:
      bun run typecheck

    Commit: feat(06-02): implement AccountOpenrouterProviderPreferenceDialog (PROV-04)
  </action>
  <verify>
    <automated>bun test packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File AccountOpenrouterProviderPreferenceDialog.tsx exists in packages/dashboard-web/src/components/accounts/
    - File exports parseProviderOrder, resolveProviderPreferenceSaveAction, syncProviderPreferenceState as named exports
    - File exports AccountOpenrouterProviderPreferenceDialog as named export (no default export)
    - File contains // FORK PATCH: Provider preferences dialog — PROV-04 comment
    - All 11+ tests from Plan 01 test file pass (bun test exits 0)
    - Cancel button label is "Discard Changes" (per UI-SPEC copywriting contract)
    - Dialog content div does NOT use overflow-y-auto (per UI-SPEC layout contract — two controls only)
    - DialogContent has className containing "sm:max-w-[500px]"
    - Switch has id="allow-fallbacks" and checked={allowFallbacks} and onCheckedChange={setAllowFallbacks}
    - index.ts exports AccountOpenrouterProviderPreferenceDialog
    - bun run typecheck exits 0
  </acceptance_criteria>
  <done>GREEN gate achieved. All tests pass. Dialog renders correctly per UI-SPEC. Typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add API client methods and wire dialog through AccountListItem, AccountList, and AccountsTab</name>
  <files>
    packages/dashboard-web/src/api.ts
    packages/dashboard-web/src/components/accounts/AccountListItem.tsx
    packages/dashboard-web/src/components/accounts/AccountList.tsx
    packages/dashboard-web/src/components/AccountsTab.tsx
  </files>
  <read_first>
    - packages/dashboard-web/src/api.ts (read the full file to find updateAccountModelMappings ~line 1301 and deleteCombo ~line 1882 as templates; find the class closing brace to know where to add new methods)
    - packages/dashboard-web/src/components/accounts/AccountListItem.tsx (read full file: find lines 55-57 for existing optional props, lines 76-80 for destructure, lines 204-206 for separator gate, lines 227-246 for model mappings dropdown item, and the lucide-react import at line 2-14)
    - packages/dashboard-web/src/components/accounts/AccountList.tsx (read full file: find lines 17-18 for props, lines 37-38 for destructure, lines 82-83 for forwarding)
    - packages/dashboard-web/src/components/AccountsTab.tsx (read full file: find lines 68-74 for dialog state pattern, lines 496-498 for open handler, lines 537-548 for async handler, line 634 for AccountList props, lines 701-712 for dialog mount pattern, and the imports at top)
    - packages/http-api/src/handlers/accounts.ts (read lines 3594-3690 to confirm: PUT handler reads body.allow_fallbacks in snake_case; DELETE handler uses accountId from URL only)
  </read_first>
  <action>
    STEP 1 — packages/dashboard-web/src/api.ts:
    Add two methods to the API class, immediately after the updateAccountModelMappings method.
    Both methods carry // FORK PATCH: OpenRouter provider preference API methods (PROV-04) above the first method.

    putAccountOpenrouterProviderPreference(accountId: string, order: string[], allowFallbacks: boolean): Promise<void>
      url = /api/accounts/${accountId}/openrouter-provider-preference
      body = { order, allow_fallbacks: allowFallbacks }  ← snake_case key, CRITICAL per accounts.ts handler
      Use this.put(url, body)
      Follow the API method logging template: startTime, logger.debug arrow, try/catch/finally, logger.debug back arrow, logger.error on error
      Error re-throw: if (error instanceof HttpError) throw new Error(error.message); throw error;

    deleteAccountOpenrouterProviderPreference(accountId: string): Promise<void>
      url = /api/accounts/${accountId}/openrouter-provider-preference
      Use this.delete(url) — NO body argument
      Same logging template as above
      Same error re-throw pattern

    STEP 2 — packages/dashboard-web/src/components/accounts/AccountListItem.tsx:
    a) Add Settings2 to the lucide-react import. If Settings2 is not available in lucide-react 1.7.0, use SlidersHorizontal instead (also available in recent lucide-react). Do not use Hash (already used for Model Mappings).
    b) Add onProviderPreferenceChange?: (account: Account) => void to the props interface, after onModelMappingsChange
    c) Add onProviderPreferenceChange to the destructure list
    d) Extend the separator gate condition: {(onCustomEndpointChange || onModelMappingsChange || onProviderPreferenceChange) && <DropdownMenuSeparator />}
    e) Add the new dropdown item immediately after the Model Mappings DropdownMenuItem block:
       {/* FORK PATCH: Provider preferences dropdown item (PROV-04) */}
       {account.provider === "openrouter" && onProviderPreferenceChange && (
         <DropdownMenuItem
           onClick={() => onProviderPreferenceChange(account)}
           title={account.openrouterProviderPreference ? `Provider order: ${account.openrouterProviderPreference.order.join(", ")}` : "Configure OpenRouter provider preferences"}
         >
           <Settings2 className={`mr-2 h-4 w-4 ${account.openrouterProviderPreference ? "text-primary" : ""}`} />
           Provider Preferences
           {account.openrouterProviderPreference && (
             <span className="ml-auto text-xs text-muted-foreground">set</span>
           )}
         </DropdownMenuItem>
       )}

    STEP 3 — packages/dashboard-web/src/components/accounts/AccountList.tsx:
    a) Add onProviderPreferenceChange?: (account: Account) => void to AccountListProps, after onModelMappingsChange
    b) Add onProviderPreferenceChange to destructure list
    c) Forward onProviderPreferenceChange={onProviderPreferenceChange} to AccountListItem (adjacent to onModelMappingsChange={onModelMappingsChange})

    STEP 4 — packages/dashboard-web/src/components/AccountsTab.tsx:
    a) Add AccountOpenrouterProviderPreferenceDialog to the import from "./accounts" (alongside AccountModelMappingsDialog)
    b) Add providerPreferenceDialog state (same shape as modelMappingsDialog):
       // FORK PATCH: Provider preferences dialog state (PROV-04)
       const [providerPreferenceDialog, setProviderPreferenceDialog] = useState<{ isOpen: boolean; account: Account | null }>({ isOpen: false, account: null });
    c) Add handleProviderPreferenceChange handler (mirrors handleModelMappingsChange):
       // FORK PATCH: (PROV-04)
       const handleProviderPreferenceChange = (account: Account) => { setProviderPreferenceDialog({ isOpen: true, account }); };
    d) Add handleSetProviderPreference and handleClearProviderPreference async handlers (mirror handleUpdateModelMappings with try/await api.method()/await loadAccounts()/catch setActionError/throw pattern)
    e) Pass onProviderPreferenceChange={handleProviderPreferenceChange} to AccountList (adjacent to onModelMappingsChange prop)
    f) Mount the dialog JSX (mirror the modelMappingsDialog mount block):
       {/* FORK PATCH: (PROV-04) */}
       {providerPreferenceDialog.isOpen && providerPreferenceDialog.account && (
         <AccountOpenrouterProviderPreferenceDialog
           isOpen={providerPreferenceDialog.isOpen}
           account={providerPreferenceDialog.account}
           onOpenChange={(open) => setProviderPreferenceDialog({ isOpen: open, account: open ? providerPreferenceDialog.account : null })}
           onSetProviderPreference={handleSetProviderPreference}
           onClearProviderPreference={handleClearProviderPreference}
         />
       )}

    After all changes, run: bun run typecheck && bun run lint
    Fix any typecheck errors before committing. Do not fix pre-existing lint errors in unrelated lines.

    Commit: feat(06-02): wire provider preferences dialog through AccountListItem/AccountList/AccountsTab (PROV-04)
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint && bun test packages/dashboard-web/src/</automated>
  </verify>
  <acceptance_criteria>
    - api.ts contains method putAccountOpenrouterProviderPreference with body using allow_fallbacks (snake_case) key
    - api.ts contains method deleteAccountOpenrouterProviderPreference with no body argument to this.delete
    - api.ts new methods are annotated with // FORK PATCH: OpenRouter provider preference API methods (PROV-04)
    - AccountListItem.tsx props interface contains onProviderPreferenceChange?: (account: Account) => void
    - AccountListItem.tsx dropdown item is wrapped in {account.provider === "openrouter" && onProviderPreferenceChange && (...)}
    - AccountListItem.tsx separator gate condition includes onProviderPreferenceChange
    - AccountList.tsx props interface, destructure, and forward all include onProviderPreferenceChange
    - AccountsTab.tsx contains providerPreferenceDialog state variable
    - AccountsTab.tsx imports AccountOpenrouterProviderPreferenceDialog from "./accounts"
    - AccountsTab.tsx mounts the AccountOpenrouterProviderPreferenceDialog with both onSetProviderPreference and onClearProviderPreference props
    - bun run typecheck exits 0
    - bun test packages/dashboard-web/src/ exits 0
  </acceptance_criteria>
  <done>Dialog fully wired. Non-OpenRouter accounts show no "Provider Preferences" item. OpenRouter accounts show it. Typecheck clean. All dashboard tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard input → API | User text in "Provider Order" field is parsed client-side and sent to PUT endpoint |
| PUT body key casing | allow_fallbacks (snake_case) in JSON body — mismatch causes silent wrong value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | PUT body key casing | mitigate | putAccountOpenrouterProviderPreference sends { allow_fallbacks: allowFallbacks } (snake_case) — verified against accounts.ts handler which reads body.allow_fallbacks |
| T-06-02-02 | Tampering | Malformed provider names | accept | Server-side validation at accounts.ts lines 3602–3614 validates each element is a non-empty string; client filter(Boolean) removes blanks |
| T-06-02-03 | Tampering | XSS via provider name in title attribute | accept | React renders title as text content (not innerHTML); no XSS risk |
</threat_model>

<verification>
bun test packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts exits 0 (GREEN gate)
bun run typecheck exits 0 across all modified files
bun test packages/dashboard-web/src/ exits 0 (all dashboard tests pass)
grep "allow_fallbacks" packages/dashboard-web/src/api.ts returns at least one match (snake_case key confirmed)
grep "account.provider.*openrouter\|openrouter.*account.provider" packages/dashboard-web/src/components/accounts/AccountListItem.tsx returns match (gate confirmed)
grep "// FORK PATCH" packages/dashboard-web/src/api.ts returns match (annotation confirmed)
</verification>

<success_criteria>
Dialog implemented. All TDD tests pass. Full prop threading in place. Types clean. "Provider Preferences" item visible only for OpenRouter accounts. Preference state indicator (orange icon + "set" badge) renders when preference is set.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-ui-maintenance-hardening/06-02-SUMMARY.md`
</output>
