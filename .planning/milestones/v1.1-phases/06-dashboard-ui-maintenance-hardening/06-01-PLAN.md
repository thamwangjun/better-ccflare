---
phase: 06-dashboard-ui-maintenance-hardening
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts
autonomous: true
requirements:
  - PROV-04
must_haves:
  truths:
    - "Failing tests exist for parse/branch logic before any implementation"
    - "Test suite verifies: empty order input → DELETE path, non-empty → PUT path"
    - "Test suite verifies: useEffect populates form from account.openrouterProviderPreference"
    - "bun test on the new file exits non-zero (RED gate)"
  artifacts:
    - path: "packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts"
      provides: "TDD RED gate — failing test stubs for dialog parse/branch logic"
      exports: []
  key_links:
    - from: "test file handleSave branch logic"
      to: "onSetProviderPreference / onClearProviderPreference mocks"
      via: "mock function assertions"
      pattern: "expect.*toHaveBeenCalledWith"
---

<objective>
Create failing test stubs for the AccountOpenrouterProviderPreferenceDialog parse/branch logic.

Purpose: Establish the RED gate. Tests must fail before implementation begins — they define the contract that Plan 02 must satisfy.
Output: One test file with three failing test cases covering parse, branch, and useEffect sync behavior.
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dashboard-ui-maintenance-hardening/06-CONTEXT.md
@.planning/phases/06-dashboard-ui-maintenance-hardening/06-VALIDATION.md

Reference existing test for structure pattern:
@packages/dashboard-web/src/components/accounts/RateLimitProgress.test.tsx
</context>

<feature>
  <name>AccountOpenrouterProviderPreferenceDialog parse/branch logic</name>
  <files>
    packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts
  </files>
  <behavior>
    Parse behavior (comma-separated input → string[]):
    - "anthropic/claude-3-5-sonnet, openai/gpt-4o" → ["anthropic/claude-3-5-sonnet", "openai/gpt-4o"]
    - "  provider1 , provider2  " → ["provider1", "provider2"] (trims whitespace)
    - "" → [] (empty string → empty array)
    - "   " → [] (whitespace-only → empty array)
    - "provider1" → ["provider1"] (single item → single-element array)

    Branch behavior (handleSave routing):
    - parsed.length === 0 → onClearProviderPreference(accountId) called, onSetProviderPreference NOT called
    - parsed.length > 0 → onSetProviderPreference(accountId, parsedOrder, allowFallbacks) called, onClearProviderPreference NOT called
    - allowFallbacks=false with non-empty order → onSetProviderPreference called with allowFallbacks=false

    useEffect sync behavior (account prop changes):
    - account with openrouterProviderPreference { order: ["p1","p2"], allowFallbacks: true } → providerOrder = "p1, p2", allowFallbacks = true
    - account with openrouterProviderPreference { order: ["p1"], allowFallbacks: false } → providerOrder = "p1", allowFallbacks = false
    - account with openrouterProviderPreference = null → providerOrder = "", allowFallbacks = true (default)
    - account = null → providerOrder = "", allowFallbacks = true (default)
  </behavior>
  <implementation>
    These are pure logic tests — no DOM rendering. Extract the parse logic and branch condition
    as standalone functions to test in isolation. The test file imports functions that do not
    exist yet (will be created in Plan 02), so bun test will fail at import time — that is
    the RED gate.

    Structure the test file as follows:
    1. Import the parse helper (parseProviderOrder) and branch resolver (resolveProviderPreferenceSaveAction)
       from the dialog component path — these imports will fail until Plan 02 creates them.
    2. Write describe("parseProviderOrder") with 5 test cases.
    3. Write describe("resolveProviderPreferenceSaveAction") with 3 test cases.
    4. Write describe("syncFromAccount") with 4 test cases for useEffect sync logic.

    Use bun:test imports: import { describe, expect, it } from "bun:test"
    No mock framework needed — test pure functions directly.

    The file must NOT import React or render components. Pure function tests only.
    This keeps bun:test working without any DOM environment setup.

    Export names to test (these will be exported from the dialog in Plan 02):
    - parseProviderOrder(input: string): string[]
    - resolveProviderPreferenceSaveAction(parsed: string[]): "set" | "clear"
    - syncProviderPreferenceState(account: Account | null): { providerOrder: string; allowFallbacks: boolean }

    Note: Use import type { Account } from "../../../api" for the Account type (pure type import, no runtime cost).
    The Account type already includes openrouterProviderPreference: { order: string[], allowFallbacks: boolean } | null.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for dialog parse/branch/sync logic (RED gate)</name>
  <files>packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts</files>
  <read_first>
    - packages/dashboard-web/src/components/accounts/RateLimitProgress.test.tsx (test structure pattern — bun:test, describe/it/expect, no DOM)
    - packages/dashboard-web/src/components/accounts/AccountModelMappingsDialog.tsx (understand parseMappingValue and handleSave branch structure to model tests against)
    - packages/types/src/account.ts (Account type — confirm openrouterProviderPreference field shape: { order: string[], allowFallbacks: boolean } | null)
  </read_first>
  <behavior>
    - parseProviderOrder("anthropic/claude, openai/gpt") → ["anthropic/claude", "openai/gpt"]
    - parseProviderOrder("  p1 , p2  ") → ["p1", "p2"]
    - parseProviderOrder("") → []
    - parseProviderOrder("   ") → []
    - parseProviderOrder("single") → ["single"]
    - resolveProviderPreferenceSaveAction([]) → "clear"
    - resolveProviderPreferenceSaveAction(["p1"]) → "set"
    - resolveProviderPreferenceSaveAction(["p1","p2"]) → "set"
    - syncProviderPreferenceState(null) → { providerOrder: "", allowFallbacks: true }
    - syncProviderPreferenceState({ openrouterProviderPreference: null }) → { providerOrder: "", allowFallbacks: true }
    - syncProviderPreferenceState({ openrouterProviderPreference: { order: ["p1","p2"], allowFallbacks: true } }) → { providerOrder: "p1, p2", allowFallbacks: true }
    - syncProviderPreferenceState({ openrouterProviderPreference: { order: ["p1"], allowFallbacks: false } }) → { providerOrder: "p1", allowFallbacks: false }
  </behavior>
  <action>
    Create the test file at packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts.

    Use bun:test built-in runner. Import: import { describe, expect, it } from "bun:test"

    Import the three functions under test from the dialog component:
      import { parseProviderOrder, resolveProviderPreferenceSaveAction, syncProviderPreferenceState } from "../AccountOpenrouterProviderPreferenceDialog"

    Import the Account type:
      import type { Account } from "../../../api"

    Write three describe blocks — "parseProviderOrder", "resolveProviderPreferenceSaveAction", "syncProviderPreferenceState" — with test cases matching the behavior list above exactly.

    For syncProviderPreferenceState tests: construct minimal Account-shaped objects with just the openrouterProviderPreference field. Cast as Account using "as unknown as Account" to avoid providing all required Account fields.

    The file must be pure TypeScript (.ts not .tsx) — no JSX, no React imports, no DOM.

    Add file header comment: // FORK PATCH: TDD RED gate for provider preferences dialog (PROV-04)

    After writing the file, run: bun test packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts

    Expected result: test run FAILS (import error or test failures). This is the RED gate — failure here is correct. If it passes, the functions already exist somewhere and the plan is wrong.

    Commit with message: test(06-01): add failing tests for provider preference dialog parse/branch logic
  </action>
  <verify>
    <automated>bun test packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts
    - File contains exactly 3 describe blocks: "parseProviderOrder", "resolveProviderPreferenceSaveAction", "syncProviderPreferenceState"
    - File contains at least 11 it() test cases covering all behavior items above
    - bun test on this file exits non-zero (RED — imports fail or assertions fail)
    - File header contains // FORK PATCH: TDD RED gate for provider preferences dialog (PROV-04)
    - No import of React, no JSX, no DOM rendering calls
  </acceptance_criteria>
  <done>Test file exists with 11+ failing test cases. bun test exits non-zero. RED gate established.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → dialog module | Test imports pure functions; no external data or network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | test assertions | accept | Test file is not production code; no user-facing trust boundary |
</threat_model>

<verification>
bun test packages/dashboard-web/src/components/accounts/__tests__/AccountOpenrouterProviderPreferenceDialog.test.ts exits non-zero (RED gate confirmed).
bun run typecheck exits 0 (test file itself is type-valid even if imports fail at runtime).
</verification>

<success_criteria>
Test file created. All 11+ tests fail (RED gate). Commit exists with message matching test(06-01): pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-ui-maintenance-hardening/06-01-SUMMARY.md`
</output>
