---
phase: 05-api-layer
plan: 02
type: tdd
wave: 2
depends_on:
  - "05-01"
files_modified:
  - packages/http-api/src/handlers/accounts.ts
  - packages/http-api/src/router.ts
autonomous: true
requirements:
  - PROV-03
must_haves:
  truths:
    - "DELETE /api/accounts/:id/openrouter-provider-preference returns 204 and sets openrouter_provider_preference to NULL in the DB (D-01: separate endpoint per REST semantics, sets column to NULL)"
    - "DELETE returns 204 No Content on success (D-02)"
    - "DELETE on a non-existent account returns 404 Not Found (D-03)"
    - "PUT handler returns 204 No Content — unchanged (D-04)"
    - "No account object in PUT or DELETE response body — 204 with null body only (D-05)"
    - "No GET /api/accounts/:id endpoint added in this phase (D-06)"
    - "PUT handler function definition carries a // FORK PATCH: annotation"
    - "DELETE handler function definition carries a // FORK PATCH: annotation"
    - "DELETE route dispatch block in router.ts carries a // FORK PATCH: annotation"
    - "DELETE route dispatch block appears before the generic account-removal block in router.ts"
    - "All 11 tests in openrouter-provider-preference.test.ts pass"
    - "bun run lint && bun run typecheck && bun run format all exit 0"
  artifacts:
    - path: "packages/http-api/src/handlers/accounts.ts"
      provides: "createAccountOpenrouterProviderPreferenceDeleteHandler export"
      exports: ["createAccountOpenrouterProviderPreferenceDeleteHandler"]
      contains: "// FORK PATCH: clear per-account OpenRouter provider preference"
    - path: "packages/http-api/src/router.ts"
      provides: "DELETE route dispatch for /openrouter-provider-preference"
      contains: "// FORK PATCH: clear OpenRouter provider preference"
  key_links:
    - from: "packages/http-api/src/router.ts"
      to: "packages/http-api/src/handlers/accounts.ts"
      via: "import createAccountOpenrouterProviderPreferenceDeleteHandler"
      pattern: "createAccountOpenrouterProviderPreferenceDeleteHandler"
    - from: "packages/http-api/src/handlers/accounts.ts"
      to: "packages/database/src/database-operations.ts"
      via: "dbOps.setAccountOpenrouterProviderPreference(accountId, null)"
      pattern: "setAccountOpenrouterProviderPreference.*null"
---

<objective>
GREEN gate: Add the DELETE handler to accounts.ts, add the FORK PATCH annotation to the existing PUT handler, register the DELETE route in router.ts — then verify all 11 tests pass.

Purpose: Satisfy PROV-03. Operators can now clear a per-account OpenRouter provider preference via DELETE. The FORK PATCH annotation on the PUT handler aligns with the convention already established in the codebase (missing since Phase 4 commit 28703cc2).

Output: Updated accounts.ts (new DELETE handler + annotation on PUT), updated router.ts (DELETE route dispatch + import).
</objective>

<execution_context>
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamw/development/local/better-ccflare/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-layer/05-CONTEXT.md
@.planning/phases/05-api-layer/05-RESEARCH.md
@.planning/phases/05-api-layer/05-PATTERNS.md
@.planning/phases/05-api-layer/05-01-SUMMARY.md

<interfaces>
<!-- Existing PUT handler — add FORK PATCH annotation above line 3594 -->
From packages/http-api/src/handlers/accounts.ts (~line 3589-3656):
  /**
   * Create an account OpenRouter provider preference update handler
   * PUT /api/accounts/:id/openrouter-provider-preference
   * Body: { order: string[], allow_fallbacks?: boolean }
   */
  export function createAccountOpenrouterProviderPreferenceHandler(
      dbOps: DatabaseOperations,
  ) { ... }

<!-- All needed imports already present in accounts.ts -->
  import type { DatabaseOperations } from "@better-ccflare/database";
  import { BadRequest, errorResponse, NotFound } from "@better-ccflare/http-common";

<!-- Existing PUT dispatch block in router.ts (~lines 619-629) -->
  // Account OpenRouter provider preference update
  if (
      path.endsWith("/openrouter-provider-preference") &&
      method === "PUT"
  ) {
      const openrouterPrefHandler =
          createAccountOpenrouterProviderPreferenceHandler(this.context.dbOps);
      return await this.wrapHandler((req) =>
          openrouterPrefHandler(req, accountId),
      )(req, url);
  }

  // Account removal  ← DELETE block MUST be inserted before this line
  if (parts.length === 4 && method === "DELETE") { ... }

<!-- DB facade method (already accepts null) -->
From packages/database/src/database-operations.ts (~line 750):
  // FORK PATCH: set per-account OpenRouter provider preference
  async setAccountOpenrouterProviderPreference(
      accountId: string,
      preference: string | null,
  ): Promise<void>

<!-- Test file written in Plan 01 -->
  packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts
  11 tests covering T-01..T-11
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DELETE handler to accounts.ts and FORK PATCH annotation to PUT handler</name>
  <files>packages/http-api/src/handlers/accounts.ts</files>

  <read_first>
    - packages/http-api/src/handlers/accounts.ts lines 3585–3656 (existing PUT handler — exact current state before any edits; read the JSDoc comment, function signature, body structure, error handling)
    - packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts (the RED gate tests — understand what the DELETE handler must do to make T-03, T-04, T-09 pass)
    - packages/http-api/src/handlers/accounts.ts lines 1–30 (confirm BadRequest, NotFound, errorResponse are already imported — no new imports needed)
  </read_first>

  <behavior>
    After this task:
    - T-03: deleteHandler(makeDeleteRequest(), accountId) on existing account → response.status === 204
    - T-04: deleteHandler(makeDeleteRequest(), accountId) on existing account → readPreference(id) === null
    - T-09: deleteHandler(makeDeleteRequest(), "nonexistent-id") → response.status === 404
  </behavior>

  <action>
    Two changes to packages/http-api/src/handlers/accounts.ts:

    Change 1 — Add FORK PATCH annotation to existing PUT handler:
    Insert the comment "// FORK PATCH: set per-account OpenRouter provider preference" on the line immediately before "export function createAccountOpenrouterProviderPreferenceHandler(" (currently ~line 3594). Do not change the JSDoc block or the function itself.

    Change 2 — Add DELETE handler immediately after the closing brace of createAccountOpenrouterProviderPreferenceHandler (currently ~line 3656):
    Add a blank line then the following function (tabs for indentation, double quotes for strings):
    - Comment: "// FORK PATCH: clear per-account OpenRouter provider preference"
    - Export function signature: createAccountOpenrouterProviderPreferenceDeleteHandler(dbOps: DatabaseOperations)
    - Returns async (_req: Request, accountId: string): Promise<Response>
    - try block:
      - const db = dbOps.getAdapter()
      - const account = await db.get<{ name: string }>("SELECT name FROM accounts WHERE id = ?", [accountId])
      - if (!account) return errorResponse(NotFound("Account not found"))
      - await dbOps.setAccountOpenrouterProviderPreference(accountId, null)
      - log.info call: "Cleared OpenRouter provider preference for account ${accountId}"
      - return new Response(null, { status: 204 })
    - catch block: log.error + errorResponse(error instanceof Error ? error : new Error("Failed to clear OpenRouter provider preference"))

    No new imports needed — DatabaseOperations, errorResponse, NotFound are already imported.
  </action>

  <verify>
    <automated>bun test packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts 2>&1 | grep -E "T-03|T-04|T-09|pass|fail|error"</automated>
  </verify>

  <acceptance_criteria>
    - grep "// FORK PATCH: set per-account OpenRouter provider preference" packages/http-api/src/handlers/accounts.ts returns a match immediately above "export function createAccountOpenrouterProviderPreferenceHandler("
    - grep "createAccountOpenrouterProviderPreferenceDeleteHandler" packages/http-api/src/handlers/accounts.ts returns at least 2 matches (comment line + function signature line)
    - grep "// FORK PATCH: clear per-account OpenRouter provider preference" packages/http-api/src/handlers/accounts.ts returns a match
    - grep "setAccountOpenrouterProviderPreference(accountId, null)" packages/http-api/src/handlers/accounts.ts returns a match
    - bun test openrouter-provider-preference.test.ts: T-03, T-04, T-09 pass (DELETE behavior correct)
    - bun run typecheck exits 0 after this change
  </acceptance_criteria>

  <done>
    DELETE handler exported from accounts.ts. PUT handler has FORK PATCH annotation. T-03, T-04, T-09 pass. No typecheck errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register DELETE route in router.ts and verify all 11 tests pass</name>
  <files>packages/http-api/src/router.ts</files>

  <read_first>
    - packages/http-api/src/router.ts lines 615–640 (exact current state of the PUT dispatch block and the Account removal block — must see both before inserting the DELETE block)
    - packages/http-api/src/router.ts lines 1–35 (current import block from "../handlers/accounts" — confirm createAccountOpenrouterProviderPreferenceHandler is already imported; add createAccountOpenrouterProviderPreferenceDeleteHandler alongside it)
    - packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts (confirm T-01 through T-11 labels and what each asserts — all 11 must pass after this task)
  </read_first>

  <behavior>
    After this task all 11 tests pass:
    - T-01: PUT valid order → 204
    - T-02: PUT persists to DB
    - T-03: DELETE → 204
    - T-04: DELETE clears DB column to null
    - T-05/T-06/T-07: PUT invalid inputs → 400
    - T-08: PUT non-existent → 404
    - T-09: DELETE non-existent → 404
    - T-10: PUT allow_fallbacks defaults to true in stored JSON
    - T-11: PUT explicit allow_fallbacks: false persisted
  </behavior>

  <action>
    Two changes to packages/http-api/src/router.ts:

    Change 1 — Add import:
    In the named import block from "../handlers/accounts" (near the top of the file), add createAccountOpenrouterProviderPreferenceDeleteHandler alongside the existing createAccountOpenrouterProviderPreferenceHandler. Keep alphabetical order of names within the import block.

    Change 2 — Add DELETE dispatch block:
    Immediately after the closing brace of the PUT dispatch block (after line ~629 ")(req, url);" closing the PUT block), insert:
    - "// FORK PATCH: clear OpenRouter provider preference"
    - if block: path.endsWith("/openrouter-provider-preference") && method === "DELETE"
    - Body: const deleteHandler = createAccountOpenrouterProviderPreferenceDeleteHandler(this.context.dbOps); return await this.wrapHandler((req) => deleteHandler(req, accountId))(req, url);

    The DELETE block must appear BEFORE the "// Account removal" block (which checks parts.length === 4 && method === "DELETE"). Verify placement by reading lines 629–638 before inserting.
  </action>

  <verify>
    <automated>bun test packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - grep "createAccountOpenrouterProviderPreferenceDeleteHandler" packages/http-api/src/router.ts returns at least 3 matches (import + comment + usage)
    - grep "// FORK PATCH: clear OpenRouter provider preference" packages/http-api/src/router.ts returns a match
    - The DELETE dispatch block appears before "// Account removal" in the file (grep line numbers confirm ordering)
    - bun test packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts: all 11 tests pass, 0 failures
    - bun run lint && bun run typecheck && bun run format all exit 0
    - bun test (full suite) passes — no regressions in other test files
  </acceptance_criteria>

  <done>
    All 11 tests in openrouter-provider-preference.test.ts pass. bun run lint && bun run typecheck && bun run format exit 0. Full test suite green. GREEN gate confirmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→router | Incoming HTTP request with accountId path param — untrusted |
| router→handler | Dispatches accountId extracted from URL path to handler function |
| handler→DB | dbOps.setAccountOpenrouterProviderPreference(accountId, null) — parameterized SQL, no injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Spoofing | DELETE /api/accounts/:id/openrouter-provider-preference | accept | API key auth is enforced at the router's outer auth middleware (pre-existing); this handler runs only for authenticated callers. No additional auth needed at this layer. |
| T-05-06 | Tampering | DELETE path — wrong handler matched | mitigate | Specific DELETE block placed before generic account-removal block (parts.length === 4 && method === "DELETE"). Path has 5 segments so generic block cannot match it, but ordering is enforced by placement. Verified in router.ts lines 631–638. |
| T-05-07 | Information Disclosure | 404 response body | accept | Returns standard "Account not found" string — no DB internals, no stack traces in production error responses. |
| T-05-08 | Denial of Service | Repeated DELETE to clear non-existent account | accept | Returns 404 immediately after single DB read; no expensive operations. Low risk. |
| T-05-09 | Elevation of Privilege | DELETE clears another account's preference | accept | accountId is extracted from authenticated request path; caller must have valid API key. No additional RBAC in this codebase at this layer — consistent with existing handler patterns. |
</threat_model>

<verification>
GREEN gate complete when:
1. bun test packages/http-api/src/handlers/__tests__/openrouter-provider-preference.test.ts — 11 passed, 0 failed
2. bun run lint exits 0
3. bun run typecheck exits 0
4. bun run format exits 0
5. bun test (full suite) passes — no regressions
6. grep confirms "// FORK PATCH:" on line before createAccountOpenrouterProviderPreferenceHandler in accounts.ts
7. grep confirms "// FORK PATCH:" on line before createAccountOpenrouterProviderPreferenceDeleteHandler in accounts.ts
8. grep confirms "// FORK PATCH:" on DELETE dispatch block in router.ts
</verification>

<success_criteria>
DELETE /api/accounts/:id/openrouter-provider-preference endpoint is live. All 11 TDD tests pass. Both fork-specific code blocks (handler + route dispatch) carry // FORK PATCH: annotations. The missing annotation on the PUT handler function definition is corrected. Full test suite green.
</success_criteria>

<output>
After completion, create .planning/phases/05-api-layer/05-02-SUMMARY.md with:
- Files modified (accounts.ts, router.ts)
- New export: createAccountOpenrouterProviderPreferenceDeleteHandler
- Test result: 11/11 pass
- FORK PATCH annotations added: list each location
- Lint/typecheck/format: all green
- Any deviations from the plan
</output>
